# Architecture — Components

Detailed per-module reference. Linked from [CLAUDE.md](../../CLAUDE.md). Update both when behaviour changes.

**Execution flow**: Queue read → provider selection (fallback chain) → profile loading → policy check → skill gating → memory context injection → prompt building → provider execution → result persistence → heartbeat.

---

## `orchestrator.py`

Main loop (`run_once`/`run_watch`), prompt building (`_build_prompt`), file change tracking via before/after snapshots. `run_watch()` calls `_log_capacity()` once after startup delay so the dashboard timeline has a fresh data point from the first second. `run_watch()` starts `start_heartbeat_thread()` so scheduled checks fire during long tasks. Tool-task `error_code="timeout"` marks retry directly without provider fallback (prevents next provider failing non-retryably → `[-]` finalization that would incorrectly satisfy `#needs:` deps). `error_code="format_error"` (tool output broke its own format contract) shares the hang path: requeue with `HANG_RETRY_BACKOFF_SEC` under the persistent `<!-- hang: N -->` counter, blocked once `MAX_HANG_RETRIES` is exceeded, and no provider cooldown (a format break says nothing about provider health). Because the two share that counter it counts *fruitless attempts*, not hangs — two format errors plus a first genuine hang block at 3 — so the log/Telegram text names the joint count (`3. erfolgloser Versuch (Hang/Format-Fehler zusammen gezählt)`) instead of the old, false `zum 3. Mal`. `error_code="provider_not_allowed"` is terminal: a `#provider` tag barred by `tool_providers` is finalized with a clear message rather than parked for a quota reset that can never unblock it. `error_code="no_provider_allowed"` is the same verdict without a tag — when `dispatcher.policy_dead_end()` reports that the allow-list and the routable chain do not intersect (a typo'd provider name, or a profile whose provider order the policy excludes), the task is finalized too. Both paths exist because `select_provider() -> None` is ambiguous: it means "nothing allowed" (permanent) and "nothing available" (temporary, park and wait) alike, and only the second is worth a quota-reset wait. `run_once()` returns `None` when all tasks are blocked by `#needs:` deps — `run_watch()` then sleeps `SLEEP_POLL_INTERVAL` (5 min) instead of 30s busy-loop, and skips `notify_queue_complete()` to avoid Telegram spam.

## `dispatcher.py`

Provider selection with fallback chain (**`_PRIORITY = ["claude", "codex"]`** since 2026-08-15), cooldown management, provider-bound model aliases: Claude (`#claude_haiku`, `#claude_sonnet`, `#claude_opus`), Codex (`#codex_5` → gpt-5.6-sol flagship, `#codex_5_4` → gpt-5.6-terra, `#codex_mini` → gpt-5.6-luna), Gemini (`#gemini_pro`, `#gemini_flash`, `#gemini_flash_lite` — still defined, no longer routed to). Without a tag the CLI's own default model is used. `dispatcher._TAG_MAP` is the canonical list of every routing tag (25 entries: 20 model-specific aliases + 5 bare provider tags); `queue_manager.MODEL_TAG_RE` derives its alternation from it, so the two cannot drift apart — see `queue_manager.py`.

**Gemini left the chain (2026-08-15).** Reason: `IneligibleTierError` on the remaining path plus data-protection concerns about the HTTP mode. The withdrawal is layered, and all three layers matter — removing only one would leave a route open: out of `_PRIORITY` (no default routing), out of `policy.yaml`'s `tool_providers` (an explicit `#gemini*` tag is refused with `provider_not_allowed` rather than honoured), and `GEMINI_API_KEY` commented out in `.env` — which is what disarms the `_limits_ok()` short-circuit below that would otherwise report Gemini unconditionally available. `providers/gemini.py` and its aliases are retained; `get_provider_by_name("gemini")` still resolves, so tests and any future re-enable keep working.

**Policy-aware lookups for tool code.** `get_provider_by_name()` is the raw, **policy-blind** registry lookup and must not be used by tools: six of them resolved their cross-provider/second-opinion providers through it and thereby bypassed `tool_providers` entirely. All seven of those call sites now go through the helpers below. The last two (`tools/brainstorm.py`, `tools/scientific_investigation_phase2.py`) are the *resolution* step rather than the allocation step, and re-checking there is the point, not redundancy: an allocation list can be restored from a persisted state file or produced by the degraded fallback path, so it outlives the policy that made it. The one deliberate exception is `tools/critical_review.py`, which pairs `policy_allows_provider()` with the raw lookup so "policy said no" and "unknown provider" get different log lines. Tool code uses instead:

| Helper | Use |
|---|---|
| `policy_allows_provider(name, tool)` | predicate — lets a caller log *why* it rejected (policy vs. unknown name) |
| `get_provider_for_tool(name, tool)` | policy-filtered single lookup; None on unknown **or** barred. Delegates to `get_provider_by_name`, so a test that stubs that one still governs what tools can resolve |
| `policy_provider_lookup(tool)` | returns a `(name) -> provider \| None` callable — drop-in default for the `provider_lookup` parameter of the cross-provider allocation phases. Candidate tuples stay unchanged; a barred provider resolves to None and drops out of the loop |
| `forced_provider_policy_violation(task, tool_name=…, …)` | `(provider, allowed)` when a `#provider` tag is barred, else None. `select_provider()` returns None for both "policy said no" and "no capacity"; only the latter is worth waiting for, so the orchestrator needs the distinction to fail terminally instead of parking forever |
| `policy_dead_end(task, tool_name=…, profile=…, …)` | the same distinction for the **untagged** case: the effective allow-list when the policy/profile layers leave *no* routable provider, else None. Shares `_selection_order()` with `select_provider()` so the verdict cannot drift from the order actually walked |

**Fail-open is asymmetric.** A missing/unreadable/corrupt `policy.yaml` reaches `get_allowed_providers()` as "no restriction" — the same value a deliberately unrestricted policy produces, and the file sits in a OneDrive-synced folder where that state is an operating reality, not a hypothetical. `dispatcher._allows()` splits it: fail-**open** for the capped providers (a lost policy must not take the orchestrator offline), fail-**closed** for `_UNCAPPED_PROVIDERS` = `{openrouter, vibe}`, the pay-per-token set that `_limits_ok()` reports as unconditionally available and that nothing else caps. One frozenset feeds both, so "no capacity gate" and "barred by default" cannot drift apart. An explicit allow-list naming them still authorises them. The section's *shape* is normalised too (`policy._coerce_provider_list`, `dispatcher._sanitize_allowed`): a scalar `dev-loop: claude` used to become `['c','l','a','u','d','e']` and a mapping its key list — both perfectly well-formed allow-lists matching nothing.

**Forced providers are filtered too.** `select_provider()` used to prepend the forced provider *after* applying the policy filter, so `#openrouter`/`#vibe` walked straight past the exact filter meant to bar them. It now returns None with a `[policy]` log line — deliberately no fallback to another provider, because a silent swap hides which model did the work, which is worse than a clean stop in an unattended run.

**Opt-in providers.** `_TAG_MAP` also carries `#openrouter`/`#or_*` and `#vibe`/`#vibe_medium`/`#vibe_small`, but `_PRIORITY` lists only `claude` and `codex` — the two pay-per-token providers are reachable by explicit tag only. Both are registered conditionally (`OPENROUTER_API_KEY` / `shutil.which("vibe")`) and `_limits_ok()` short-circuits to `True` for them since cclimits tracks no quota there. That short-circuit is the *definition* of "uncapped", which is why `_UNCAPPED_PROVIDERS` feeds both it and the fail-closed branch of `_allows()`. `_limits_ok()` carries a third short-circuit for `gemini` when `GEMINI_API_KEY` is set (the REST API exposes no pollable quota) — dormant while the key stays commented out, and a thing to remember before re-enabling Gemini, since it would make Gemini uncapped without being in `_UNCAPPED_PROVIDERS`.

**`_REVIEWER_ONLY = {"vibe"}`.** Tag resolution normally uses `_providers.get(name)`, so a tag for an unregistered provider falls through to the default chain. That is fine for OpenRouter (executor replaced by executor) but wrong for a reviewer: `_tags_unregistered_reviewer_only(task)` runs before the fallback and makes `select_provider()` return `None`, so the task is parked. A request for a non-writing second opinion must never be answered by a file-writing executor. Precedence detail: an explicit forced provider (`resolve_forced_provider`) wins, so the guard only fires when no other provider was explicitly demanded.

## `queue_manager.py`

Obsidian MD queue parsing with sidecar `.lock` file locking (msvcrt on Windows, fcntl on Unix). Regex-based metadata extraction (`cwd:`, `#tool:`, `#agent:`, `#parallel`, `#claude_*`, `#id:`, `#needs:`, `#pass1:`, `#pass2:`, etc.). `extract_pass_providers()` returns `{1: "claude", 2: "gemini"}` for cross-provider tool support. UTF-8 with cp1252 fallback. Smart wikilink/file context injection with TF-IDF section extraction. `_parse_subtask_line()` shared helper used by both `read_queue_items()` and `_replace_open_task_line()`. Subtask-aware task matching in queue mutations (mark_done/mark_retry/finalize) prevents wrong-task collisions in parallel queues. Two-pass dependency resolution in `read_queue_items()`: Pass 1 collects open tasks, Pass 2 resolves `#needs:` deps against completed IDs (`_collect_completed_ids()` scans `[x]`/`[-]` lines). Blocked tasks get `QueueTask.blocked_reason != ""` and are skipped by `run_once()` without marking done.

**`MODEL_TAG_RE` covers all 20 model aliases (fixed 2026-08-15; found 2026-07-23).** The alternation is built from `dispatcher._TAG_MAP` — every key whose tag text does not equal its own provider name (i.e. every model-specific alias, not the five bare provider-selection tags) — sorted longest-first so a prefix alias (`codex_5`) cannot shadow the longer alias it prefixes (`codex_5_4`). `extract_model_tag()` therefore now resolves `#gemini_flash_lite`, `#codex_5`, `#codex_5_4`, `#vibe_medium`, `#vibe_small` and all nine `#or_*` tags too, not just the original six. `queue_linter.py` calls the same extractor, so its cross-provider leakage check (`model_provider_mismatch`) now also covers those. Regression test: `tests/test_queue_manager_regressions.py::test_model_tag_re_covers_every_dispatcher_alias`.

## `providers/vibe.py`

Mistral Vibe CLI — second non-Claude voice next to Codex, and the queue-side counterpart to the interactive workflow's stage-2 reviewer. Registered only when `shutil.which("vibe")` finds the binary (`VibeProvider.is_available()`), and kept out of `dispatcher._PRIORITY`, so it runs only on an explicit `#vibe`/`#vibe_*` tag or as a `#second_opinion:` value. `_limits_ok()` short-circuits it like OpenRouter (pay-per-token, no cclimits quota); `--max-price` (`config.VIBE_MAX_PRICE_USD`) is the per-run cost ceiling vibe enforces on itself.

**Reviewer, not executor.** `read_only=True` → `--disabled-tools "*"` (pure reasoning over the piped prompt, `VIBE_READONLY_MAX_TURNS=1`). Otherwise `--enabled-tools read_file --enabled-tools grep` and nothing else — never `bash`/`edit`/`write_file`, and never `--auto-approve` (both read tools are `permission = "always"` in vibe's config, so no approval prompt can stall a headless run). This is a deliberate policy choice, not a CLI limitation.

The same policy is enforced one level up in `dispatcher._REVIEWER_ONLY`: a task that tags `#vibe` while Vibe is *not* registered yields **no provider at all** (`select_provider` → `None`, task parked) instead of falling through to the default chain. For an unregistered `#or_*` tag that fall-through is harmless — executor replaced by executor — but silently answering "review this without writing" with a file-writing executor widens the blast radius past what the task asked for.

**Read confinement — measured, not assumed (2026-07-23).** With `--trust` + `--workdir X`, `read_file` reads files inside X, and refuses both an absolute path outside X and a `../` traversal (positive control included: the in-workdir read succeeded in the same session). So the working directory is the read boundary; no writing tool is enabled in either mode.

**CLI facts, all verified against vibe 2.22.0 (2026-07-23) — each one silently breaks the run if dropped:**
- **`-p` with no value reads the prompt from stdin.** Task prompts here reach ~100 KB; as an argv element that would exceed the Windows command-line limit (8191 chars under cmd.exe). Verified with an 808 KB prompt whose marker was on the last line. This also puts vibe under the same `_StdinDelivery` contract as the other CLI providers — `stdin_incomplete` handling is copied from `codex.py` verbatim, including the "real error wins at `rc != 0`" ordering.
- **`--trust` is mandatory** — without it the CLI blocks on the trust prompt for an untrusted cwd (`~/.vibe/trusted_folders.toml` lists the vault as untrusted) and the run hangs at 0 % CPU with no output.
- **There is no `--model` flag.** The model lives in `~/.vibe/config.toml` (`active_model`) and is overridable per process via `VIBE_ACTIVE_MODEL`, whose value is a vibe *alias* (`mistral-medium-3.5`, `devstral-small`), not a raw model name. An unknown alias falls back to the default **without an error** — hence `config.VIBE_MODEL_ALIASES` as the single source of those strings.
- **`PYTHONUTF8=1` in the child env** — vibe is a Python CLI and dies with a `charmap` codec error writing non-ASCII output under a Windows cp1252 locale.
- **`shell=False`** — real `.exe` on PATH (uv tool shim), not an npm `.cmd` wrapper, so no cmd.exe quoting sits between us and the argv.
- **No usage data.** `--output json` echoes the full message list including vibe's multi-KB system prompt and still carries no token or cost fields, so text mode (stdout = the final answer, nothing else) is strictly better. `RunResult` leaves the token fields at 0 and the orchestrator's char/duration estimation takes over, same as Codex.

Model forcing goes through `run_with_watchdog(..., env=…)`, which passes a **fully merged copy** of `os.environ` to `Popen` (Popen replaces the environment, it does not merge). Building that copy in the provider rather than mutating `os.environ` is load-bearing: providers are shared singletons driven from `parallel_runner` threads, so an in-place mutation would leak one run's model into a concurrent run.

## `providers/base.py`

`BaseProvider` ABC with per-provider `_lock` for cooldown state and `threading.local()` for per-thread forced model. `RunResult` includes `input_tokens`/`output_tokens` for capacity estimation **plus `cache_creation_input_tokens`/`cache_read_input_tokens`** (Anthropic prompt-cache fields, populated only by Claude provider — used for billing analytics, NOT for 5h/7d quota gates). Class-level `supports_sessions: bool = False` capability flag — Claude overrides to True; Codex, Gemini, OpenRouter and Vibe stay False. `run()` accepts `session_id` and `resume` parameters that providers without session support silently ignore.

## `providers/process_runner.py`

Liveness/hang watchdog shared by all four CLI providers (DRY). `run_with_watchdog(cmd, *, input_text, cwd, idle_timeout, hard_timeout, shell, liveness_lines=False, env=None, stdin_via_file=False, ...) → WatchdogResult` (CompletedProcess-compatible via `returncode`/`stdout`/`stderr`, plus `stdin_error`).

**`env`** is passed straight to `Popen`, which **replaces** the child environment rather than merging it — callers must hand over a fully merged copy of `os.environ`, never a delta. Building that copy per call (as `providers/vibe.py` does for `VIBE_ACTIVE_MODEL`) instead of mutating `os.environ` is load-bearing: providers are shared singletons driven from `parallel_runner` threads, so an in-place mutation would leak one run's model into a concurrent run.

**stdin delivery verification (`_feed_stdin` / `_StdinDelivery`):** `_spawn` opens stdin with `text=True` → a buffered `TextIOWrapper`, so `write()` only fills the buffer and the *tail* is flushed by `flush()`/`close()`. A failed tail flush therefore truncates the END of the prompt — and `orchestrator._build_prompt` assembles `core → skills → memory → task`, i.e. the task text sits last. Result: the CLI answers a context-only prompt ("what would you like me to do?"), exits 0 with a `subtype=="success"` result event, and the run used to be finalized as a SUCCESS that did nothing. Observed 5× (2026-07-14/15/20), cost 3 days of vault health data. `_feed_stdin` now flushes explicitly before closing (so the tail flush is its own attributable failure site), logs via the module logger, and records the outcome in `_StdinDelivery`. **Fail-closed:** `delivered` is set ONLY after a successful `close()`; a caught error, an uncaught exception class (`MemoryError`, `RuntimeError` at shutdown) and a thread that never finished all count as failed delivery. `run_with_watchdog` reads it after the joins and surfaces it as `WatchdogResult.stdin_error`; the timeout path attaches it to the raised `TimeoutExpired` as `exc.stdin_error`. **Blind spot — why detection alone was not enough:** the feeder confirms delivery from the WRITE side only. A prompt that fits under the OS pipe buffer (~64 KB) — OR the ≤64 KB *tail* of a larger prompt once the child stops reading — lands in the kernel buffer, so `write`/`flush`/`close` all succeed and NO `stdin_error` is raised even though the child never consumed those bytes. This is not merely a small-prompt edge. **Correction 2026-07-25:** the morning-brief incidents (20./24./25.07.) were previously attributed to exactly this tail loss — that attribution is **disproven**. The prompts that actually arrived were measured from the Claude Code session files: success and failure runs are structurally identical (22.026–22.888 chars, not ~70 K tokens) and the task text was fully present in **all** of them, at 61–64 % of the prompt rather than at the end. The real defect was the prompt LAYOUT (see [ADR-002](../adr/002-task-last-in-prompt.md)); `_build_prompt` now appends the task last under `## Aufgabe`. The blind spot described above is real for the feeder path and worth guarding — it just was not what broke the morning-brief.

**File-based delivery (`stdin_via_file`, `_write_temp_prompt`):** when set, `run_with_watchdog` writes `input_text` to a temp file (UTF-8, `newline=""`), opens it read-only and hands it to the child as stdin — no feeder thread. The whole prompt is on disk before the first read and the child reads to a real end-of-file, so there is no buffered close/flush race and no dependency on the OS pipe buffer size; the tail cannot be lost. **Delivery is verified, not assumed** (`_verify_prompt_file`): the byte count on disk is compared against `len(input_text.encode(encoding))`, and a mismatch, an empty payload or an unreadable file surfaces as `stdin_error` exactly like a feeder failure. Until 2026-07-25 this path instead set `delivered = True` unconditionally ("True by construction"), which pinned `stdin_error` to None for every caller and made the detection in `claude.py` dead code — a safety net that could not fire. The temp file is closed + unlinked in the `finally` on every path (success, kill, spawn failure). **The claude provider opts in** (`providers/claude.py`, together with `shell=False`); the piped feeder + `stdin_error` detection stays the default for Codex/Vibe, which have no observed incident — widening it to them is a separate change with its own blast radius.

**Provider contract:** all three CLI providers read `getattr(result, "stdin_error", None)` — `getattr` because the provider tests fake this object with a three-field `SimpleNamespace` — and turn it into the **bare code** `stdin_incomplete` (exact-equality matching is the repo-wide convention; detail goes to `output`). It is applied in exactly two places, never up front: **(1) inside the success branch** — a run that would otherwise be booked as success, which is the incident shape — and **(2) as a last resort at `rc == 0`** when no better classification matched. Ordering is load-bearing in BOTH directions: a child dying early (rate limit, missing session, "not logged in", a panic) ALSO breaks the pipe because the prompt (~100 KB) dwarfs the OS pipe buffer, so checking too early masks the real cause, while trusting the fallback at `rc != 0` sweeps ordinary CLI crashes into the stdin path. At `rc != 0` the real error therefore always wins; the delivery diagnosis is carried alongside it — appended to `output` on the generic fallback (the keyword returns for `rate_limit`/`session_missing`/`unreachable` keep their bare shape), and always present in the `process_runner` log.

**Reach — known limits.** The guarantee covers single-shot queue tasks. It does NOT extend to: (a) `#parallel` parents, which `orchestrator.py` finalizes regardless of subtask success, so a truncated subtask prompt closes the parent anyway; (b) multi-phase tools that translate a provider error into their own `phase*_failed` code and lose `stdin_incomplete` on the way. Both are pre-existing behaviours for every error code, not specific to this one — widening them is a separate change with its own blast radius.

**Orchestrator reaction:** `stdin_incomplete` is in the `_run_with_retry` bail-out list — retrying the same oversized prompt down the same pipe within one run is pure token burn (~26k cache_creation tokens per attempt). Beyond that it is deliberately **not** special-cased and takes the generic error path (5-min provider cooldown + rotation), which is bounded and self-healing: once all providers are cooled, `select_provider` returns None and the task is parked via `mark_retry`. An earlier attempt to skip cooldown+rotation — motivated by the fact that a local pipe fault says nothing about provider health and all providers share the feeder — removed that throttle without replacing it and produced an unbounded 30-second retry loop. Revisiting this needs the `hang` treatment (persistent counter in the queue line, backoff, BLOCK after N), not a bare `break`. **Analytics caveat:** because the single-shot path rotates providers, its replay record is written as `provider_unreachable` once all providers are exhausted — so `CAT_STDIN` shows up in the dashboard mainly for forced-provider and tool tasks. The real code still reaches Telegram (`notify_error`) and `orchestrator.log`. Do not "fix" this by special-casing the error in the orchestrator; that is exactly the reflex that produced two P1 regressions.

**Why:** plain `subprocess.run(..., timeout=)` only kills the direct child; on Windows with `shell=True` the real node/claude grandchild is orphaned, keeps stdout pipes open and `communicate()` blocks until the orphan finishes (observed: nominal kill 900 s, return 1094 s).

**Threading model:** `Popen` + TWO daemon reader threads draining stdout AND stderr simultaneously (a single reader deadlocks on a full OS pipe buffer of the other stream) + one daemon stdin feeder **on the piped-delivery path only** (`stdin_via_file=True` hands the child a temp file as stdin and starts NO feeder — see the file-based delivery note above). Readers use the underlying buffer's `read1()` (byte-granular) + a `codecs` incremental decoder, NOT line iteration — a line iterator only registers activity on a newline, so a newline-less progress burst (build logs, `pytest` dots) would look idle and get false-killed. The watchdog loop (`_watchdog_loop`, 0.5 s poll) returns `"idle"` (no activity for `idle_timeout`) or `"hard"` (`hard_timeout` elapsed); on either it tree-kills the process, joins readers (5 s timeout — the kill closes the orphan FDs so readers get EOF), and raises `subprocess.TimeoutExpired` with an extra `timeout_kind` attribute. daemon=True is the backstop if an FD stays open. A `try/finally` closes `proc.stdout/stderr/stdin` after the joins on every path (success and kill) — `subprocess.run` did this via its context manager; without it each call leaks 2-3 pipe FDs → exhaustion in `--watch`. Each reader caps its retained sink at `_MAX_CAPTURE_CHARS` (~8 MB, oldest lines dropped first, tail kept so the final `result` line survives) — `stream-json --verbose` streams every `tool_result` (full file reads, Bash output) as its own event, so an uncapped sink could hold hundreds of MB on a long phase.

**Tree-kill (`_tree_kill`):** Windows `taskkill /F /T /PID` fired twice (belt-and-suspenders against a grandchild forking between `poll()` and enumeration); POSIX `os.killpg(SIGTERM)` → grace → `SIGKILL` (process spawned with `start_new_session=True`). Always `proc.wait()` afterwards so no zombie remains.

**Liveness model (`_Liveness`):** byte mode (Gemini/Codex, `liveness_lines=False`) touches last-activity per stdout/stderr line. NDJSON-event mode (Claude, `liveness_lines=True`) parses each stdout line. **There is NO top-level `{"type":"tool_use"}` event** (verified claude 2.1.158, Win11) — tool activity lives in the *content blocks* of `assistant`/`user` events. `_Liveness.on_event(evt, now)` inspects `message.content[]` via `_content_blocks()` and tracks a **SET of open `tool_use` ids** (`_open_tool_ids`, plus an `_anon_open` counter for blocks lacking an `id`): an `assistant` event adds each `tool_use` block's `id`; a `user` event discards each `tool_result` block's `tool_use_id`; the top-level `result` event (`_TOOL_DONE_EVENTS`) clears all. `idle_for()` returns 0 (idle timer paused) while the set is non-empty. A set rather than a single boolean is essential: Claude emits **parallel** `tool_use` blocks in one assistant message and spawns `Agent` subagents, so the first `tool_result` must not resume the timer while a sibling tool is still running (would false-idle-kill a productive run). A `threading.Lock` guards the multi-step set updates against the watchdog reader. Real sequence: `system/init → rate_limit_event → assistant[thinking] → assistant[tool_use] → (stdout silence for the tool duration) → user[tool_result] → assistant[text] → result`. This is load-bearing: a single blocking tool call holds stdout silent for its whole duration (verified 12 s tool → 18.1 s inter-line gap, scales with tool duration), so a naive byte watchdog would kill a productive pytest/build/install phase. `hard_timeout` stays the absolute backstop. Constants: `_POSIX_TERM_GRACE_SEC=5`, `_WATCHDOG_POLL_SEC=0.5`, `_READER_JOIN_TIMEOUT_SEC=5`, `_TASKKILL_TIMEOUT_SEC=10`.

## `providers/claude.py`

Uses `--output-format stream-json --verbose` to emit one NDJSON event per line (init/assistant/tool_use/result) — incremental liveness signal for the watchdog (`liveness_lines=True`) — plus actual token usage. `_extract_result_event()` returns the LAST `type=="result"` event (zeilenbasiert; a single-object json payload parses as one line, keeping legacy parsing paths valid); `_parse_json_response()` reads its `.result` + `.usage` into all four token fields (field-parity preserved for quota calibration/analytics). `--exclude-dynamic-system-prompt-sections` moves per-machine sections (cwd, env, git status) from system-prompt into the first user message → static system-prompt enables Anthropic 1h prompt-cache hits. `--allowedTools` always includes `Agent` (deep-security-audit subagent fan-out). **This was `Task` until 2026-07-30 — a name that does not resolve to any tool.** The subagent-spawning tool is `Agent`; every `Task*` name in the tools reference is a task-LIST tool (TaskCreate/Get/List/Output/Stop/Update). An unresolvable entry in `--allowedTools` never matches, so the Agent tool was left un-allowlisted on the read-only path; headless runs cannot answer a permission prompt, so the fan-out was denied and silently degraded to the monolithic single-perspective run this allowlist exists to prevent. Neither the test suite nor the heartbeat model check could see it. On the write path `--dangerously-skip-permissions` masked the effect entirely. rate_limit/session_missing keyword detection runs on **signal-bearing surfaces only** — `stderr`, dedicated `rate_limit_event` / `error` NDJSON lines, the result event's `subtype`, and the result text **only when the result is an error** (`subtype != "success"`) — NOT a success result's answer prose (which would false-positive a `rate_limit` cooldown on a task merely mentioning "rate limit"/"quota"). Session flags as before (`--session-id`/`--resume`, gated by `CLAUDE_SESSION_ENABLED`). idle-kill → `error="hang"`, hard-kill → `error="timeout"` (via `TimeoutExpired.timeout_kind`). `supports_sessions = True`. Passes `--model <id>` when `_forced_model` is set, and `--effort <level>` when `_forced_effort` is set — from the `#effort:` queue tag, values per `config.CLAUDE_EFFORT_LEVELS` (`low|medium|high|xhigh|max`, verified against `claude --help` 2026-07-30). Both use the same thread-local property + `finally`-restore discipline in `providers/base.py`. `--effort` is **Claude-exclusive by construction**: the property sits on `BaseProvider` but only this provider reads it, so Codex/Gemini/Vibe/OpenRouter ignore an `#effort:` tag without needing a capability check. Absent a tag the flag is omitted entirely — the CLI's own session default must not be silently overridden from here. An unknown level is reported by `queue_linter` (`unknown_effort`) and degrades to the session default at runtime rather than failing a scheduled run. **Prompt delivery:** `shell=False` (claude resolves to a native `.exe`, so no cmd.exe layer sits between us and the child) + `stdin_via_file=True` (temp-file stdin, deterministic EOF — see `process_runner.py`). This is the fix for the silent prompt-tail loss where a large prompt's trailing task text was dropped and the run was booked `success` (2026-07-20 detection was insufficient, recurred 2026-07-24). The post-run success evaluation and `stdin_error` handling are unchanged; `stdin_error` is simply always None on the file path.

## `providers/gemini.py`

**Dual-mode** since the consumer Gemini CLI (Code Assist for individuals / AI Pro / AI Ultra) was shut down 2026-06-18.

**HTTP-API mode (`GEMINI_API_KEY` set, preferred):** calls the Google Gemini REST API directly via `urllib.request` (stdlib, like `openrouter.py`) — `POST {GEMINI_BASE_URL}/models/{model}:generateContent` with `x-goog-api-key` header. The whole `task` (which already carries the system prompt + SAFETY_RULES from `get_system_prompt`) is sent as the single user turn — no separate `systemInstruction` (would duplicate safety). Output from `candidates[0].content.parts[].text`; tokens from `usageMetadata` → `promptTokenCount`=input, `candidatesTokenCount + thoughtsTokenCount`=output (gemini-3.x are thinking models; thinking is billed as output), `cachedContentTokenCount`=cache_read. `GEMINI_MAX_OUTPUT_TOKENS` (default 16384) sized so heavy thinking doesn't starve the visible answer. Error mapping: 429→`rate_limit`+cooldown, 401/403→`auth_error`, 5xx/URLError→`unreachable`, `TimeoutError`→`timeout`, invalid JSON / no candidates→`parse_error`, `promptFeedback.blockReason` or blocked `finishReason` (SAFETY/RECITATION/…)→`model_refusal`, empty text + MAX_TOKENS→hint to raise the cap. `_forced_model` (resolved GA id, e.g. `gemini-3.5-flash`) selects the model. `read_only` is a no-op (REST API has no tool/exec surface).

**Legacy CLI mode (no key):** Standard/Enterprise subscription users retain CLI access — `--yolo` full tool access, `--approval-mode default` read-only, `--model <id>` forced, through `run_with_watchdog` byte-only (`CLI_IDLE_TIMEOUT_NO_LIVENESS_SEC`); `timeout_kind` maps idle→`hang`, hard→`timeout`.

**Retired from every active path (2026-08-15).** Both modes above are still implemented and still work if re-enabled, but nothing routes here: `gemini` is out of `dispatcher._PRIORITY`, out of `policy.yaml`'s `tool_providers`, and `GEMINI_API_KEY` is commented out in `.env`. Trigger was an `IneligibleTierError` on the remaining tier plus data-protection concerns about sending review content through the HTTP API.

The availability machinery below is therefore **dormant, not deleted** — and it is the reason the commented-out key matters rather than being cosmetic. In HTTP mode the public REST API exposes no pollable subscription quota, so availability was cooldown-driven: `dispatcher._limits_ok("gemini", …)` returns `True` whenever a key is set, `limits._needs_token_refresh` skips the dead CLI/OAuth refresh, and `limits._apply_gemini_http_snapshot`/`_apply_gemini_http_override` report a synthetic available snapshot instead of the cclimits quota — the same treatment as OpenRouter. With a key set, Gemini is thus an *uncapped* provider that is **not** in `dispatcher._UNCAPPED_PROVIDERS`, so it would not inherit the fail-closed policy default the other two uncapped providers get. Anyone re-enabling Gemini has to decide that question deliberately.

## `providers/codex.py`

Uses `codex exec` with `-c approval_policy=never` + `--sandbox <mode>`. Passes `--model <id>` (e.g. `gpt-5.6-luna`) when `_forced_model` is set. Runs through `run_with_watchdog` byte-only (`CLI_IDLE_TIMEOUT_NO_LIVENESS_SEC`); `timeout_kind` maps idle→`hang`, hard→`timeout`.

## `providers/openrouter.py`

HTTP-based provider (OpenAI-compatible chat completions, `urllib.request` stdlib — no `requests` dep). Pay-per-token, never in fallback chain — only activated via explicit `#openrouter` or `#or_*` tag. Conditionally registered in `dispatcher._providers` ONLY when `OPENROUTER_API_KEY` env var is set; otherwise tagged tasks fall through to the default chain (claude/gemini/codex). `supports_sessions = False`. Token extraction via `usage.{prompt_tokens, completion_tokens, prompt_tokens_details.cached_tokens}` → `RunResult.{input_tokens, output_tokens, cache_read_input_tokens}`. Error mapping: HTTP 429 → `rate_limit` + cooldown, 401/403 → `auth_error` (no cooldown, config issue), 5xx/URLError → `unreachable`, JSON parse fail → `parse_error`, 200-with-error-body → `api_error`. `read_only` accepted but no-op (provider has no tool execution surface). Designed for single-call non-agentic tasks (heartbeat LLM-check, memory summaries, telegram `/chat`) — DO NOT route agentic tools (dev-loop, review-loop, deep-security-audit) here. `dispatcher._limits_ok("openrouter", ...)` is a hardcoded `True` since pay-per-token has no subscription quota tracked by cclimits.

## `policy.py`

`PolicyEngine` singleton — AUTO/APPROVE/DENY classification from vault YAML, blocks on `threading.Event` for Telegram approval. **Tool Contracts (P3)**: same `policy.yaml` file has a `tool_contracts:` section parsed at load time into `dict[str, ToolContract]`. `get_tool_contract(name) → ToolContract` resolves specific entry → `default` entry (with rewritten `tool_name`) → empty contract (never None). `ToolContract` dataclass: `tool_name`, `max_iterations`, `max_runtime_sec`, `max_files_touched`, `stop_conditions: tuple[str, ...]`, `reporting_path: str`. Zero/negative budget values get normalised to None so the resolved contract is always sane. `list_tool_contracts()` returns a snapshot for `--doctor`. `_parse_tool_contract()` ignores unknown keys silently; Doctor's `_validate_tool_contracts()` surfaces typos as WARN-level output without blocking the run.

## `usage_suggester.py`

`UsageSuggester` singleton — proactive task suggestions when provider capacity is underutilized. Same threading pattern as PolicyEngine.

## `memory.py`

TF-IDF + temporal decay search over past task results stored in vault. Auto-archival after 180 days. `append_lesson()`/`get_lessons_context()` re-enabled with LLM-generated summaries for tool loops (review-loop, dev-loop) to capture generalized patterns and root causes. `get_lessons_context(tool_name, cwd)`: CWD-filtered injection — `| * |` entries are universal (always injected), project-specific entries only when CWD matches. `append_lesson(universal=True)` writes `*` as project. Semantic dedup via TF-IDF similarity (`_LESSON_DEDUP_THRESHOLD=0.45`) at write time prevents redundant entries. `get_context_for_task()`: CWD-preference — wenn ≥2 same-CWD Treffer über Threshold, werden cross-project Matches verworfen. `append_daily_log()`: 80-Zeichen First-Line-Summary (statt 300 chars). **Leerlauf-Filter (2026-08-19):** Ergebnisse, in denen das Modell meldet, es finde keine Aufgabe, werden aus beiden Injektions-Layern ferngehalten — `_is_noninformative()` (Layer 3, verlangt Absageformel **und** ≤`MEMORY_NOOP_MAX_OUTPUT_TOKENS`; `output_tokens == 0` heißt „unbekannt" und filtert nicht) greift in `search_memory()` und `_get_recent_memories()`, damit ein Leerlauf keinen top_k-Slot belegt; `_is_noop_log_entry()` (Layer 2) greift beim Schreiben in `append_daily_log()` und beim Lesen in `get_daily_context()`. Beide filtern zur **Lesezeit**, also ohne Migration auch für Altbestand; das Urteil über den Lauf bleibt bei `#verify:` (ADR-002, Nachtrag). Der zitierte Aufgabentext vergangener Läufe ist in beiden Layern auf `MEMORY_TASK_LABEL_CHARS` gekürzt — bei 80 bzw. 120 Zeichen reproduzierte er den Anfang der laufenden Queue-Zeile.

## `heartbeat.py`

Scheduled health checks with mtime-reloading config from vault `HEARTBEAT.md`. 14 built-in handlers (`HANDLER_KEYS`, matched by first substring hit — `queue-healing` is listed before `queue` on purpose): queue-healing, queue-idle, git-status, disk-space, check-limits, log-capacity, summarize, stale-branch, usage-suggest, session-cleanup, model-check, skill-suggest, **status-recap** (P6 — daily 24h Telegram summary via `analytics.last_24h_summary()` + `_format_recap()`; idle day → one-liner "Keine Aktivität in 24h"; daily idempotency handled by `HeartbeatRunner`'s `last_run_date` check), **check-ci-failures** (P4 — delegates to `ci_watcher.sweep_once()`; returns `None` on quiet sweeps to suppress notification noise, formats summary only when items were queued OR gh errors hit). `_INTERVAL_RE` accepts `## Every N minutes/hours/days` (days = N×1440 minutes) so monthly cadences work via `## Every 30 days`. `## Every 0 X` sections are dropped at parse time with a warning (without that the items would silently inherit `interval_min=0` and be treated as daily). `_check_session_cleanup()` reads the sidecar registry (`session_registry.py`), prunes entries older than `ORCH_SESSION_RETENTION_DAYS=14`, and deletes matching `~/.claude/projects/**/<uuid>.jsonl` files via glob — interactive Claude Code sessions are NEVER touched because they're not in the registry. `_check_model_updates()` runs Phase A (CLI-Probe via `_probe_model`: minimal `read_only=True` ping per alias; SKIPS providers in cooldown to avoid stacking errors; transient keywords `_PROBE_TRANSIENT_KEYWORDS` cover BOTH typed errors `rate_limit` AND raw stderr `rate limit` plus auth/login/credential/expired/token; dead keywords `_PROBE_DEAD_KEYWORDS` are narrow phrases like "model not found", "unknown model", "no such model" — avoids false positives like "unsupported encoding") + Phase B (LLM-Heuristik via `_llm_check_for_newer_models`: TWO-TIER provider selection — **Tier 1**: OpenRouter (free model, `$0/call`) if `OPENROUTER_API_KEY` is configured and not in cooldown; **Tier 2**: existing default-chain fallback via `select_provider(exclude={"openrouter"})` when OpenRouter fails or is unavailable. Single prompt with TODAY'S DATE as anchor; the prompt lists ALL five alias dicts — including the pay-per-token `OPENROUTER_MODEL_ALIASES` and `VIBE_MODEL_ALIASES` that Phase A skips, since one LLM call costs the same regardless of list length. Strict OK match `text.upper().rstrip(".").strip() == "OK"` rejects "Okay,..." prefix; LLM-call FAILURE returns visible `⚠️ LLM-Check failed: <error>` instead of being silently swallowed). Returns `None` when everything is current → no Telegram spam. **Persistent state** (`logs/heartbeat-state.json`): items with `interval_min >= 1440` (≥ 1 day) survive orchestrator restarts so a `## Every 30 days` model-check doesn't fire on every `--watch` start (cost saving). `HeartbeatRunner._lock` prevents concurrent `run_due()` calls (non-blocking acquire → skip if already running). `start_heartbeat_thread(heartbeat, queue_read_fn, stop_event, poll_sec=60)` launches a daemon thread so checks fire on time even when the main thread is blocked for hours inside a long task.

## `analytics.py`

Parses task results, log files, queue events into `TaskRecord`/`LimitSnapshot`/`QueueEvent`/`ToolTraceEvent` dataclasses. **`last_24h_summary(now=None)` (P6)**: pure aggregation returning `{done, failed, provider_counts, top_successes, top_failures, pending_approval, blocked_count, idle}` — newest-first task list, max 3 entries per top list, provider name normalised by stripping `+tool` suffix (`claude+dev-loop` counts under `claude`). Pulls pending-approval state from `policy.PolicyEngine.has_pending_approval()` and blocked-task count from `queue_manager.read_queue_items()` (filters `blocked_reason != ""`). Defensive: import / lookup failures degrade silently, never raise. `TaskRecord` includes the four token fields (input/output + cache_creation/cache_read) parsed from frontmatter. Aggregation functions + `get_dashboard_data()` with 30s TTL cache. `_billing_cost_units(records)` returns weighted token-units (input×1, cache_creation×1.25, cache_read×0.1, output×5 — Anthropic billing weights, NOT quota weights) for cost-trend visualization; `_cache_hit_rate(records)` returns cache_read / (input + cache_creation + cache_read) as percentage. Both NOT used for quota gating (limits.py owns that). `_get_current_limits()` reads the most-recent-per-provider snapshot from `logs/capacity-log.md`; result exposed as `current_limits` in dashboard API. Dashboard data exposes `billing_recent`/`billing_total`/`cache_hit_rate_recent`/`cache_hit_rate_total`. Queue events read from `logs/queue-events.log`.

**`_parse_tool_traces(allowed_roots, max_age_days=30, max_events_per_file=1000)`**: globs `<root>/**/.<tool>/traces/*.jsonl` over `ALLOWED_CWD_ROOTS`, skips files older than 30 days, caps events per file. Tolerates invalid JSON lines (skip + continue).

**`_tool_trace_stats(events)`**: aggregates per-tool `{runs, completed_runs, success_runs, avg_duration_sec, total_events}`. A run is "completed" if its run_id has a `run_end` event; "success" when `details.success is True`. Exposed as `tool_trace_stats` in dashboard data — absence of `run_end` for a run_id signals an incomplete/crashed run (analytics feature, not bug).

## `dashboard.py`

Standalone HTTP server (default port 8211, `DASHBOARD_PORT`-overridable; `_bind_server` auto-falls back to a free port if the preferred one is taken or Windows-reserved → `WinError 10013`) serving Chart.js dashboard. `GET /` HTML, `GET /api/data` JSON (full payload, 30s cache). Auto-refresh 60s. Also launchable via `--dashboard` flag.

**Active-Runs-Panel**: Direkt unter Header — Live-Tabelle laufender Tool-Runs (Tool / Task / Iter N/M / Phase / Provider / Tokens in/out/cache_read / Elapsed). Datenquelle: `ActiveRunRegistry.list_active()` über `logs/active_runs/<run_id>.json`. Eigener Polling-Tick alle 30s über `GET /api/data?only=active_runs` (umgeht den 30s-Cache des Vollbild-Endpoints für minimal-latente Updates). Stale-Records (>6h ohne Update) werden grau/kursiv mit gelber Live-Dot dargestellt; Records >24h werden beim nächsten Read physisch gelöscht.

## `scripts/safety_hook.py`

Claude Code `PreToolUse` hook — hard-denies dangerous Bash commands using `SAFETY_DENY_PATTERNS` from config.py. Works even in `--dangerously-skip-permissions` mode. Registered in `.claude/settings.local.json`.

## `config.py`

Centralized constants (~70+), `.env` loader (no external dotenv), mtime-cached `SOUL.md` personality loader, provider-bound model aliases: `CLAUDE_MODEL_ALIASES`, `GEMINI_MODEL_ALIASES` (`gemini_pro` → `gemini-3.1-pro-preview`, `gemini_flash` → `gemini-3.5-flash`, `gemini_flash_lite` → `gemini-3.5-flash-lite`; drift-checked GA ids), plus `GEMINI_API_KEY`/`GEMINI_BASE_URL`/`GEMINI_DEFAULT_MODEL`/`GEMINI_MAX_OUTPUT_TOKENS` for HTTP-API mode, `CODEX_MODEL_ALIASES` (`codex_5` → `gpt-5.6-sol`, `codex_5_4` → `gpt-5.6-terra`, `codex_mini` → `gpt-5.6-luna`), `OPENROUTER_MODEL_ALIASES` (4 free: `or_minimax_free` → `minimax/minimax-m2.5:free`, `or_deepseek_free`, `or_qwen_free`, `or_nemotron_free`; 5 paid flagships from Chinese open-source families: `or_glm` → `z-ai/glm-5`, `or_kimi` → `moonshotai/kimi-k2.6`, `or_qwen` → `qwen/qwen3-max`, `or_deepseek` → `deepseek/deepseek-v4-pro`, `or_minimax` → `minimax/minimax-m2.7` — IDs verified against OpenRouter `/models` API).

`VIBE_MODEL_ALIASES` (`vibe_medium` → `mistral-medium-3.5`, `vibe_small` → `devstral-small`) holds vibe's *own config aliases*, not raw model names — the provider passes the value through `VIBE_ACTIVE_MODEL` and vibe resolves it. All five dicts are joined in `_MODEL_ALIASES_BY_PROVIDER` (20 aliases total).

**Env vars**: `OPENROUTER_API_KEY` (required for provider registration), `OPENROUTER_BASE_URL` (default `https://openrouter.ai/api/v1`), `OPENROUTER_DEFAULT_MODEL` (used when no `_forced_model` is set). Vibe: `VIBE_MAX_PRICE_USD` (default 0.50 — vibe interrupts itself above it), `VIBE_MAX_TURNS` (12, tool mode), `VIBE_READONLY_MAX_TURNS` (1 — with all tools disabled a single turn is all that can happen); the price goes through `_parse_positive_float_env`, the turn budgets through `_parse_int_env` — both fall back to the default on a non-numeric or non-positive value instead of handing garbage to the CLI. `model_id_for_provider(tag, provider_name)` resolves only if tag matches its owning provider (prevents cross-provider model ID leakage during fallback). `is_known_model_tag()` for unknown-tag warnings.

**Safety**: `SAFETY_DENY_PATTERNS`: shared list of `(regex, description)` tuples used by both the Claude Code hook (hard block) and prompt injection (soft block for Gemini/Codex). `SAFETY_RULES`: compact 4-line hardcoded string (replaces per-pattern generated list — reduces prompt overhead ~150 → ~40 tokens).

**Token / capacity**: `PROMPT_DAILY_LOG_TOKENS=500` (was 1500; daily log entries now 80-char first-line summaries instead of 300-char). `MIN_CAPACITY_PERCENT` env-configurable (`MIN_CAPACITY_PERCENT=10` default; override via `.env`). Per-window thresholds: `CLAUDE_FIVE_HOUR_MIN_CAPACITY_PCT`, `CLAUDE_SEVEN_DAY_MIN_CAPACITY_PCT`, `CODEX_PRIMARY_MIN_CAPACITY_PCT`, `CODEX_SECONDARY_MIN_CAPACITY_PCT`. `CAPACITY_LOG_FILE = Path(__file__).parent / "logs" / "capacity-log.md"` (moved from vault to repo `logs/`).

**Tool timeouts / budgets**: Dev-loop: Research 3600s, Exec 7200s, Quality Review 3600s, Resolution Review 1800s. Critical-Review 3-pass: `TOOL_CR_PASS1_TIMEOUT_SEC=2400`, `TOOL_CR_PASS2_TIMEOUT_SEC=1800`, `TOOL_CR_PASS3_TIMEOUT_SEC=1800`, `TOOL_CR_PASS1_MAX_INJECT_CHARS=30000`, `TOOL_CR_MAX_PLAN_CHARS=50000`. Deep-Security-Audit: `TOOL_DSA_AGENT_TIMEOUT_SEC=1800`, `TOOL_DSA_SYNTHESIS_TIMEOUT_SEC=2400`, `TOOL_DSA_FIX_TIMEOUT_SEC=3600`, `TOOL_DSA_MAX_AGENT_OUTPUT_CHARS=15000`, `TOOL_DSA_MAX_TOTAL_INJECT_CHARS=80000`. Brainstorm: `TOOL_BS_PHASE0_TIMEOUT_SEC=600`, `TOOL_BS_PERSONA_TIMEOUT_SEC=900`, `TOOL_BS_SYNTHESIS_TIMEOUT_SEC=1800`, `TOOL_BS_MAX_ITERATIONS=5`, `TOOL_BS_CONVERGENCE_THRESHOLD=0.20`, `TOOL_BS_CLUSTER_SIMILARITY_THRESHOLD=0.40`, `TOOL_BS_MIN_PERSONAS=4`, `TOOL_BS_MAX_PERSONAS=6`, `TOOL_BS_DEFAULT_TOP_N=5`, `TOOL_BS_MAX_IDEAS_PER_PERSONA_PER_ROUND=10`, `TOOL_BS_MAX_PEER_CHARS_PER_PERSONA=8000`, `TOOL_BS_MAX_TOTAL_INJECT_CHARS=50000`.

**Cleanup constants**: `MEMORY_MAX_AGE_DAYS=30`, `MEMORY_ARCHIVE_DELETE_DAYS=90`, `MEMORY_DAILY_LOG_RETENTION_DAYS=30`, `MEMORY_LESSONS_RETENTION_DAYS=180`, `QUEUE_EVENTS_LOG_FILE`, `QUEUE_EVENTS_LOG_RETENTION_DAYS=30`, `QUEUE_DONE_MOVE_HOURS=48`, `QUEUE_DONE_DELETE_DAYS=7`.

## `limits.py`

`cclimits` wrapper for provider capacity checks, OAuth refresh handling. Direct `cclimits` invocation (no npx); disk-cache via `--cache-ttl 600` limits API calls to ~6/h.

**Polling-Intervalle**: `_BG_POLL_IDLE_SEC=600` (10 min, queue empty) — matches `cclimits --cache-ttl=600` so each idle poll bypasses the disk cache and hits the real API; `set_queue_idle(False)` wakes the bg thread on task arrival so the cache is fresh before the next task starts. `_BG_POLL_AVAILABLE_SEC=300` (5 min, queue active) — covers within-batch drift between consecutive tasks in run_once; rate_limit errors trigger `get_limits(force_refresh=True)` synchronously, so cache staleness can never delay error recovery.

`set_paused(True/False)`: while paused the bg refresh thread skips all cclimits calls and only wakes on resume — `run_watch()` calls this around its `/pause` wait so the orchestrator stops burning the monitoring quota during user-pauses.

HTTP 429 resilience: (tier 0) local JSONL fallback via `claude-monitor` (no HTTP, `CLAUDE_PLAN` env var), (tier 1) snapshot cache with estimated usage tracking, Telegram notifications on 429 start/clear. During the snapshot-cache fallback, per-task estimated usage is split across Claude's 5h/7d windows by calibrated `tokens_per_pct` ratios (`_estimate_window_usage_calibrated` — see `quota_state.py` below) rather than the reset-time heuristic; uncalibrated providers keep the heuristic.

## `quota_calibration.py` / `quota_state.py`

Calibrate a local `tokens_per_pct` quota model against `cclimits`, then expose a single-source-of-truth state file — so the orchestrator depends less on the undocumented, rate-limited `api.anthropic.com/api/oauth/usage` endpoint ([anthropics/claude-code#31637](https://github.com/anthropics/claude-code/issues/31637)).

### Phase 0 — calibration telemetry (`quota_calibration.py`)

Each successful `cclimits` poll in `_bg_refresh_loop` schedules (async, single-worker pool — never blocks the refresh loop) one CSV row per Claude window (`five_hour`, `seven_day`) to `logs/quota-calibration.csv`, pairing the real utilization-% with JSONL-derived token counts.

- **Window-aware aggregation:** `window_start = reset_at - window_size` from cclimits' `resets_in`; JSONL entries filtered on `timestamp >= window_start` (not a rolling "last N hours"), so a fresh block does not inherit the previous block's tokens. JSONL loaded once per poll (7d-sized + 10 %/6 h drift buffer), filtered per-window in memory.
- **Schema v2** (`schema_version` first column rejects mixed-version files): `schema_version, timestamp_utc, window, claude_plan, queue_idle_at_sample, cclimits_pct_used, cclimits_pct_remaining, reset_in_sec, window_start_utc, tokens_input, tokens_output, tokens_cache_creation, tokens_cache_creation_1h, tokens_cache_creation_5m, tokens_cache_read, tokens_inputoutput_only, tokens_weighted_billing, tokens_per_pct_io_only, tokens_per_pct_with_cc, tokens_per_pct_all, entries_count, models, flag_rolling_fallback, flag_low_pct, flag_cm_unavailable, note`.
- **Flags** (skip for ratio analysis): `flag_rolling_fallback` (cclimits `resets_in_sec=0`, synthetic start), `flag_low_pct` (`pct_used < 0.5%`, division noise — ratio columns left empty), `flag_cm_unavailable` (claude-monitor missing/broken — cclimits side still preserved).
- **Sample rate:** 5 min active / 10 min idle (`_BG_POLL_AVAILABLE_SEC` / `_BG_POLL_IDLE_SEC`); overlapping submissions dropped. Hard skip when Claude has any error string or no window data. Requires `claude-monitor>=3.0.0` (optional → `flag_cm_unavailable=true`).
- **Limitations:** single-machine (local JSONL ≠ cclimits' account-wide sum), single-orchestrator (CSV thread-safe, not process-safe — override `QUOTA_CALIBRATION_LOG_FILE` per instance), JSONL flush latency (active long tool calls briefly lag cclimits — `queue_idle_at_sample=false` rows), cache sub-tier mix.

**Result (2026-05-27, ~6 days, 558 usable samples, plan = max5):** `io_only` (input + output) won — 7d CV 6.9 % vs. `with_cc` 10.1 %, `all` 10.7 %. 5h noisier (best 21.6 %) but that is low-pct division noise — 5h `io_only` CV falls to 15–18 % in the ≥60 % band where gating matters, with a stable median. Both windows favour low/zero cache weight → cache tokens barely count against the rate limit. **Conservative low-percentile factors are plan- and workload-specific** (env-overridable, NOT universal): **5h ≈ 5400, 7d ≈ 75000** effective tokens per 1 %.

**Refuted 1h/5m tier-split:** the plan's fallback (split `cache_creation` into ephemeral 1h/5m tiers via direct JSONL parse if the lumped cache ratio was unstable) was built as a one-time analysis (`quota_calibration_backfill.py`, dedup key `(message.id, requestId)`, 0.00 % median deviation vs. claude-monitor) and rejected: even a grid-optimal weighting only reached 17.7 % CV on the 5h window (gate 15 %). Not wired into production; the `tokens_cache_creation_1h/5m` CSV columns stay empty.

### Phase 1 — SoTH state + calibrated estimation (`quota_state.py`)

- **`logs/cc_quota_state.json`** (`CC_QUOTA_STATE_FILE`): `_bg_refresh_loop` writes it atomically (temp file + `os.replace`, mirroring `idempotency.py`) after each poll — `schema_version`, `fetched_at_unix/utc`, per-provider/window `remaining_pct`/`used_pct`/`resets_in_sec`/`reset_at_epoch`, plus an embedded `calibration` block (model + per-window `tokens_per_pct`). `read_quota_state` / `state_age_sec` for read-only consumers (statusline, `--check-limits`). Never raises; no import of `limits` (avoids a circular dependency). The write hook is synchronous (tiny JSON, no JSONL load) and wrapped in `try/except`.
- **Calibrated per-window estimation:** `config.ESTIMATE_TOKENS_PER_PCT_CLAUDE_WINDOWS = {five_hour: 5400, seven_day: 75000}` (env vars `ORCH_TOKENS_PER_PCT_CLAUDE_5H` / `_7D`). `limits._estimate_window_usage_calibrated(provider, base, pct)` splits a headline estimate via `per_window = (pct × scalar) / window_tpp` — the headline scalar cancels, so `estimate_task_usage_pct`'s float contract and the `ESTIMATE_TOKENS_PER_PCT["claude"]` scalar are unchanged. Used by `report_estimated_usage` in the 429-fallback path. Providers without a calibration-table entry keep the reset-time heuristic (`_estimate_window_usage`).
- **Statusline (external):** renders `5h X% · 7d Y% · ⏳ reset` from the SoTH file, falling back to its prior ccusage source when the file is missing or > 20 min old. Script lives outside this repo (personal config).

### Phase 2 — live between-poll rebalancing (opt-in)

Closed-loop estimation against cclimits, flag-gated (default OFF — it changes capacity gating in normal operation; conservative factors lean toward over-gating, the safe side). Both parts only act when `ORCH_QUOTA_LIVE_ESTIMATE=true`.

- **(a) Live between-poll estimate:** `report_estimated_usage` accumulates each task's calibrated per-window usage into `_live_estimated_usage` (normal mode; the 429 path is unchanged). `get_limits()` / `get_cached_provider_pct` / `is_cached_provider_available` subtract it at serve time via `_apply_live_estimate` (recomputing per-window remaining + `available`), so back-to-back tasks see consumption immediately instead of a static cached value. Each successful poll re-anchors via `_reset_live_estimate()` (the fresh reading already reflects that consumption). The SoTH file is rewritten on each task (`_write_live_quota_state`) so the statusline reflects the live decrement. Lock order is always `_limits_cache_lock` → `_live_estimate_lock`.
- **(b) Auto-recalibration** (`ORCH_QUOTA_AUTO_RECALIBRATE=true`, requires (a)): once per day the bg loop calls `_maybe_recalibrate` → `quota_calibration.recalibrate_claude_factors(csv, defaults, min_samples, clamp, percentile)`, which schema-aware-parses the running CSV (sidesteps a stale header), filters flagged rows, takes the conservative `percentile`th (default P25) `io_only` tokens-per-pct per window, requires `≥ min_samples` (default 60) per window, clamps each factor to `[default/clamp, default*clamp]` (default 3×), and is all-or-nothing across the two Claude windows. On success it updates the live factors via `limits.set_calibrated_windows` (held in `_active_calibrated_windows` under `_active_calibration_lock`); otherwise the frozen defaults stay. Env: `ORCH_QUOTA_RECAL_MIN_SAMPLES`, `ORCH_QUOTA_RECAL_CLAMP`, `ORCH_QUOTA_RECAL_PERCENTILE`.

## `logging_setup.py`

Rotating file logger (5MB, 3 backups) + console output.

## `doctor.py`

18 setup validation checks, `--fix`/`--yes` auto-repair mode. `check_vibe_cli()` reports the optional Vibe CLI — WARN only when the binary is literally *not found in PATH*, so an installed-but-broken CLI keeps its real (FAIL) status instead of being masked as "not installed". `check_model_aliases()` runs the same `_probe_model` from `heartbeat.py` against every entry in CLAUDE/GEMINI/CODEX_MODEL_ALIASES (pay-per-token aliases are deliberately excluded — a probe per `--doctor` run would be a recurring charge) — concurrent (ThreadPoolExecutor, `_forced_model` is `threading.local()` on the singleton providers, so per-thread model selection is safe). FAIL → at least one ID rejected as unknown/deprecated; WARN → only transient errors (rate_limit/timeout) so verification was inconclusive; PASS → all aliases respond live. Doctor intentionally skips Phase B (LLM-Heuristik) — that lives only in the heartbeat to keep `--doctor` fast.

## `shutdown.py`

Shutdown state machine with countdown, cancellation via Telegram or new queue tasks.

## `notifier.py`

Telegram notifications. `_truncate()` default raised to 3500 chars (was 300); task text in start/done notifications up to 300 chars (was 100).

## `telegram_listener.py`

Telegram bot listener. AI chat mode (`/chat`) builds context-aware prompt: SOUL.md system prompt + `memory.get_daily_context()` + user question. `_handle_message()` parses the command BEFORE the shutdown-pending check so a plain-text message that cancels a pending shutdown does NOT also get forwarded to AI chat (regression guard: `parsed_command` was previously referenced before assignment, the resulting `NameError` was swallowed by the surrounding try/except, and the message was sent to AI chat anyway).

**Slash tool-commands** (`/review`, `/security`, `/audit`, `/dev`, `/critique`, `/brainstorm` — catalog in `_SLASH_TOOL_COMMANDS`) translate to `- [ ] <description> cwd:<path> #tool:<name>` queue lines. Resolution order for cwd: (1) explicit `cwd:` tag in args, (2) positional arg for `/review`/`/security`/`/audit` (subject IS the cwd), (3) parent dir of plan file for `/critique`, (4) per-chat RAM-only `_last_cwd_per_chat` dict. CWD validated via `extract_cwd()` (existence + `ALLOWED_CWD_ROOTS`). Idempotency via `idempotency.check_and_record("telegram_slash", task_line, message_id)` — Telegram retries of the same update are dropped. Slash-tool commands share the `_task_limiter` (10/min) with `/task`.

**Scientific-Investigation routing extension**: `_route_to_si_manager(args, response)` parses `<run_id> [criterion_id] [reason]` und routed an PreRegApprovalManager wenn pending; `/approve` versucht ZUERST den Manager-Pfad und fällt erst dann auf PolicyEngine zurück (legacy `/approve [category]` bleibt unverändert). Neuer `/reject <run_id> [criterion_id] [reason]` Command.

## `queue_linter.py`

Offline validator for `agent-queue.md`. CLI entry `python orchestrator.py --lint-queue`. Public API: `lint_queue(content=None) -> list[LintFinding]`, `format_findings(findings) -> str`, `exit_code_for(findings) -> int` (0 clean / 1 warnings only / 2 errors). Checks per open task: invalid/missing `cwd:` (via `extract_cwd`), unknown `#tool:<name>` (against `tools.list_tools()`), unknown model alias (against `_MODEL_ALIASES_BY_PROVIDER`), cross-provider model leakage (e.g. `#claude_opus` + explicit `#gemini` = error), `#effort:` via `_check_effort_tag` — unknown level (`unknown_effort`), a form the strict `EFFORT_TAG_RE` cannot see and which is therefore silently "no tag" at runtime (`malformed_effort`, found with a permissive probe like `_check_at_tag`), duplicate tags where only the first applies (`effort_duplicate_tag`), and a level on a task routed to a non-Claude provider (`effort_non_claude`, warning — `--effort` is Claude-only, so the tag is a no-op there); this check also runs over **subtasks**, because `SubTask.effort` honours the tag while the other per-task checks only see the parent line, duplicate `#id:` in open queue (error), `#needs:` pointing to unknown IDs (warning), `#openrouter`/`#or_*` without `OPENROUTER_API_KEY` (warning — task falls back to default chain), `#parallel` with 0-1 subtasks (warning) or shared CWD (info). Empty task text = error. HTML comments on the line (error): `html_comment_in_body` when one sits inside the task text — once the line ends in a comment (on an `#every:` task at the latest after the first appended retry marker), `OPEN_TASK_RE` ends the text at the first `<!--` and the rewrite drops the swallowed tags — and `html_comment_trailing` when a line-end comment is not a marker the parser reads. This is the only check that inspects the **raw** line: `_iter_open_tasks` yields it as a 4th tuple element because `task_text` comes from the same truncating regex and is blind to the defect. `_TRAILING_MARKER_RE` is composed from `RETRY_TAG_RE`/`HANG_COUNT_RE` so linter and parser cannot drift apart. Tool-registry import failure is non-fatal (skips unknown-tool check). Sorts findings by `(line_no, level)`. Zero LLM calls — pure validation.

**Not covered yet:** the unknown-alias regex enumerates `claude|gemini|codex|or` prefixes, so `#vibe_*` tags are neither validated nor flagged, and there is no counterpart to the OpenRouter "missing key" warning for a `#vibe` tag without the CLI (which the dispatcher answers by parking the task — see `_REVIEWER_ONLY`). This is now a **linter-only** gap: the `MODEL_TAG_RE` half was fixed 2026-08-15, so `extract_model_tag()` resolves `#vibe_medium`/`#vibe_small` and the model really is forced at runtime — the linter just cannot vet the tag. The remaining fix is a separate prefix list in `queue_linter._MODEL_ALIASES_BY_PROVIDER`, not a change to the shared extractor.

## `idempotency.py`

Duplicate-trigger deduplication for external sources (Telegram slash-commands, future webhooks/cron). JSONL store at `logs/idempotency.jsonl` (overridable via `set_store_path()`). Key = `sha256(source + canonical_payload + bucket_ts)` where canonical_payload uses `json.dumps(..., sort_keys=True)` so dict-order doesn't matter. Public API: `check_and_record(source, payload, bucket_ts) -> bool` (True = new+recorded, False = duplicate), `is_recorded(...)` (read-only check), `compute_key(...)`, `prune_old(max_age_days=30)`, `reset_for_tests()`. 30-day retention, pruning runs lazily once per calendar day. Atomic rewrite via temp file + `os.replace`. Corrupt JSONL lines are skipped on read and preserved on prune (better to keep unknown data than delete). Per-process `threading.Lock` serializes file mutations.

## `run_orchestrator.ps1`

Crash-resistant watchdog wrapper. Compatible with both Windows PowerShell 5.1 and PowerShell 7+ — uses `Where-Object` for crash-window filtering instead of the `[System.Predicate[datetime]]` ScriptBlock-cast (which only works in PS 7+). Restarts `python orchestrator.py --watch` on non-zero exit, exponential backoff (10s → 5min cap), crash-loop guard (≥5 crashes / 10 min → 30 min cooldown + Telegram alert). Reads `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` from `.env` for restart notifications via `Invoke-RestMethod` (no Python dependency for alerting). `Get-EnvValue` matches Python's `_normalize_dotenv_value` rules: strips UTF-8 BOM on first line, requires whitespace before `#` for unquoted-value comments (so URLs/hashes containing `#` survive), supports quoted values with optional trailing comments. Telegram-Token is URL-encoded via `[uri]::EscapeDataString` and stripped of control chars defensively. Logs to `logs/watchdog.log` with rotation at 10 MB → `watchdog.log.1`. Exit 0 (Ctrl+C, /shutdown) ends the loop cleanly.

## `tools/base_tool.py`

`BaseTool._runtime_deadline()` returns a monotonic deadline from `policy.get_engine().get_tool_contract(name).max_runtime_sec` (fallback `TOOL_DEFAULT_MAX_RUNTIME_SEC=3600`) — bounds the SUM of all phases/iterations so a high per-call `#timeout:` hard backstop cannot bind 10+ hours of wall-clock. `BaseTool._phase_cap(task_timeout, phase_default)` returns `min(task_timeout, phase_default)` (or `phase_default` when no task timeout) — a high task timeout is an upper deckel only and never raises a phase above its `TOOL_*_TIMEOUT_SEC` constant. All three iterative loop tools (review-loop, dev-loop, **test-loop**) check the deadline at each iteration start and bail with `error_code="tool_runtime_exceeded"` (partial output). The orchestrator tool-loop treats `tool_runtime_exceeded` as **terminal**: it finalizes the task with the partial result instead of falling back to the next provider or `mark_retry`'ing — both would restart the loop from iteration 1 with a FRESH deadline (3× the budget across providers, unbounded across polls), defeating the wall-clock bound.

`BaseTool` ABC + `_build_system_prompt(provider_name, memory_context, tool_name, cwd)` assembles system prompt with 4 memory layers ordered by stability for max prompt-cache reuse: (1) provider system prompt (SOUL+SAFETY, static), (2) curated MEMORY.md, (3) lessons (cwd-filtered, stable per tool+cwd), (4) daily log (grows during the day), (5) task-specific TF-IDF memory_context. **Lessons before Daily** is intentional — daily grows over the day and would otherwise break the prefix cache for tool+cwd reruns.

`ToolResult` carries the same four token fields as `RunResult`. `TokenCounter` helper (`.add(result)` / `.as_kwargs()`) aggregates tokens across phases — used by review-loop and dev-loop, other tools may adopt incrementally.

**`SessionContext`** helper (`SessionContext.create(provider, tool_name, cwd, cap=5)` + `.first_call_kwargs()` / `.resume_kwargs()` / `.bump()` / `.needs_rollover()` / `.rollover()`) manages CLI session lifecycle for tools that share conversation history across phases. Activates only when `provider.supports_sessions and config.CLAUDE_SESSION_ENABLED`; otherwise transparently returns empty kwargs (callers fall back to today's stateless pattern). Auto-registers UUIDs in the sidecar registry. All tool subclasses pass `tool_name=self.name, cwd=cwd` for CWD-filtered lesson injection.

**`ToolTracer`** helper (`ToolTracer.create(tool_name, cwd)` + `.emit(action, **details)`) writes one JSONL event per phase/iteration/subprocess to `{cwd}/.{tool_name}/traces/{uuid}.jsonl`. Silent no-op when `cwd is None` or directory creation fails — never breaks the tool. Suggested action vocabulary: `run_start`/`run_end`, `phase_start`/`phase_end`, `iteration_start`/`iteration_end`, `subprocess_call`/`subprocess_result`, `session_rollover`, `capacity_exhausted`. Open-ended `details` dict for tool-specific fields. Consumed by `analytics._parse_tool_traces()` for dashboard aggregation.

**`ActiveRunRegistry`** — zentraler Live-Index laufender Tool-Runs unter `logs/active_runs/<run_id>.json` (file-per-run für lock-free parallelen Zugriff). Wird automatisch über `ToolTracer.emit()` befüllt — Tool-Code muss nichts ändern, nur die emit-Vocabulary verwenden. Lifecycle: `run_start` → `ActiveRunRegistry.start(run_id, tool, task, cwd, provider)`; `iteration_start`/`iteration_end`/`phase_start`/`subprocess_result` → `update(run_id, iteration_current=…, phase=…, tokens_delta=…)` (Tokens werden akkumuliert, nicht überschrieben); `run_end` → `end(run_id)` löscht das File. Atomic writes via `tempfile + os.replace`. `list_active(stale_after_sec=21600, cleanup_after_sec=86400)` liest alle Records, markiert `status="stale"` ab 6h ohne Update und löscht physisch ab 24h. Konsumiert von `analytics._load_active_runs()` → Dashboard. Tests isolieren den Pfad über `conftest.py` `_isolate_active_runs_dir` Fixture (autouse).

## `session_registry.py`

Append-only JSONL whitelist of orchestrator-created Claude session UUIDs at `logs/orchestrator-sessions.jsonl`. `register_session(uuid, tool, cwd)` appends an entry; `is_orchestrator_session(uuid)` checks membership; `prune_old(retention_days)` splits into kept/expired and rewrites the registry. Used by the heartbeat session-cleanup handler to distinguish orchestrator sessions from interactive Claude Code sessions (which we MUST NOT delete) — both live under `~/.claude/projects/<encoded-cwd>/<uuid>.jsonl`.

## `tools/research_qa.py`

`ResearchQATool` — 3-phase read-only pre-implementation workflow (Discovery → Analysis → Questions). Output to `{cwd}/.research-qa/`. Registered as `#tool:research-qa`.

## `tools/review_loop.py`

`ReviewLoopTool` — iterative code review fixing **P1 + P2** (max 20 iterations). **P3 is non-blocking** (changed 2026-07-30): findings are split into `blocking_findings` / `deferred_p3`; the success gate is `no_findings or not blocking_findings`, only blocking findings reach the fix prompt, and the deferred P3 are appended once to the final output as an offer. `deferred_p3` is an ordered `dict[str, None]` **outside** the iteration loop — a per-round list dropped a round-1 P3 as soon as round 2 came back clean, i.e. exactly the case the "collected once at the end" promise exists for. Rationale: touching working code for cosmetics widens the diff without functional gain, and because each round re-reads the fresh diff, every P3 fix can surface new P3 — the loop would feed itself. `tools/dev_loop.py` had the split already (`blocking_findings` there gates `quality_ok`) and now accumulates + emits the same closing offer.

**Scope of the P3 exclusion:** it is a *prompt* guarantee, not a context guarantee. With `CLAUDE_SESSION_ENABLED=true` the review and fix calls share one conversation (`SessionContext` + `--resume`), so filtering `findings_text` removes P3 from the new prompt but not from the resumed history — the writing call can still see a P3 named in an earlier review. Closing that would mean a fresh session before every write call, forfeiting exactly the prompt-cache benefit session reuse exists for; declined deliberately. What holds in both modes: no P3 is ever *requested*, and the success gate counts only blocking findings, so a P3 can never keep the loop running. The same applies to `dev_loop.py`, where sessions span all phases.

The success gate is guarded against self-contradicting reviewer output by `_is_clean_output(text, findings)`: output carrying both `- [P2] …` and the `No P1/P2/P3 findings.` sentinel used to pass, because `_is_no_findings_output()` alone satisfied the left side of the `or` while a real blocking finding sat unfixed. The findings win now — the sentinel counts only when nothing parsed as a finding. `_is_no_findings_output()` stays the raw sentinel detector; both loops (and the second-opinion merge) call `_is_clean_output()`.

Infinite-loop detection via finding signature dedup — the signature is built from the **blocking** set, so a round in which only the P3 list shifted cannot read as "new findings" and defeat the no-progress detection; the drift-check counters and `previous_findings_count` use the blocking count for the same reason. Fix prompt is split into stable prefix (`_FIX_PROMPT_STABLE`, cached across iterations) + volatile suffix (`_FIX_PROMPT_VOLATILE` with iteration-specific findings + lessons hint at the end) — Anthropic prompt cache matches the longest identical prefix, so volatile content lives at the end.

**Phase B sessions**: `SessionContext` shares conversation history across review→fix→review iterations with `cap=5` rollover; review prompt always reads `git diff` fresh, so a fresh-session continuation is seamless. Fallback on `session_missing` resets to a fresh UUID and retries once.

**Second-Opinion** (opt-in via `#second_opinion:<alias>` tag, e.g. `#second_opinion:or_glm`): in iteration 1 only, after the primary review, a non-agentic LLM (typically OpenRouter via `or_*` alias) receives the pre-loaded git diff + the primary's findings and lists *additional* P1/P2/P3 issues. Resolved via `_resolve_second_opinion(alias)` which maps model aliases to their owning provider (`or_*` → openrouter, `claude_*` → claude, `vibe_*` → vibe, …) and resolves through `dispatcher.get_provider_for_tool(name, "review-loop")` — policy-aware, so an alias barred by `tool_providers` yields None and the phase is skipped instead of reaching a provider the policy bars (under the current `review-loop: [claude]` that makes `codex`/`vibe`/`or_*` inert here; `claude_*` still resolves). Bare provider names are accepted from `_SECOND_OPINION_BARE_PROVIDERS = {openrouter, claude, codex, vibe}` — Gemini is deliberately absent (retired as a reviewer, fallback executor only). `#second_opinion:` is the ONE path that honours every alias, because it carries its own regex and resolution instead of `MODEL_TAG_RE`. Extra findings are union-merged into the finding list (exact-string dedup) **before** the blocking/P3 split, so a second-opinion P1/P2 reaches the fix prompt while a second-opinion P3 only joins the deferred offer — a P3-only second opinion therefore does not reopen the loop (it sets `no_findings = False`, but the gate asks for blocking findings). Non-fatal skips: SO provider in cooldown, diff fetch fails, diff > `TOOL_RL_SECOND_OPINION_MAX_DIFF_CHARS` (30k), or `_resolve_second_opinion` returns None — primary findings continue alone. Fix phase stays on Claude (or whatever the primary provider is) — second-opinion is read-only. Timeout: `TOOL_RL_SECOND_OPINION_TIMEOUT_SEC` (10 min).

**Drift-Check (Goal-Adherence-Guard)**: Mini-LLM-Call zwischen Review und Fix, prüft ob die Findings noch zum Original-Task gehören oder die Iteration in unrelated Refactoring abgedriftet ist. Modi via `policy.yaml` `tool_phases.review-loop.drift_check_mode`: `auto` (default — Trigger ab iter≥3 mit wachsenden Findings, Halbzeit, oder iter≥5 als Sicherheitsnetz), `always` (jede Iteration), `skip`. Output strikt `ON_TOPIC: <reason>` oder `DRIFTED: <reason>` (via `_parse_drift_check`). Bei `DRIFTED` wird eine Refocus-Warning in den **nächsten** Fix-Prompt-Volatile-Block injiziert — Loop wird nicht abgebrochen. Drift-Call-Fehler sind non-fatal. Scope-Guard im `_FIX_PROMPT_STABLE` (immer aktiv, kein Cache-Bust): instruiert Fix-Agent off-topic Findings zu skippen und als `SKIPPED (off-topic): <finding>` zu markieren. Timeout: `TOOL_RL_DRIFT_CHECK_TIMEOUT_SEC` (2 min, Mini-Prompt ohne Codebase-Exploration).

## `tools/dev_loop.py`

`DevLoopTool` — Research+Plan → Execute → Quality-Review + Resolution-Review (`RESOLVED`/`PARTIAL`/`UNRESOLVED`) loop until both reviews pass (max 20 iterations).

**Phase 1 (Research) and Phase 1.5 (Plan) are merged into a single subprocess call** (`_RESEARCH_AND_PLAN_PROMPT` produces both sections; saves ~42 k cache_creation tokens vs. the prior two-call pattern). Skip-mode (`policy.tool_phases.dev-loop.plan_approval=skip`) falls back to research-only via `_RESEARCH_ONLY_PROMPT`. State persisted in `{cwd}/.dev-loop/state.json` under phase `research_and_plan_done` (or `research_done` for skip-mode), enabling capacity-exhausted retry without re-running the merged phase.

**Phase B sessions**: `SessionContext` shares conversation history across all phases when enabled, with `cap=5` rollover (fresh UUID after every 5 iterations to bound conversation length — explicit findings re-injection in `review_context` keeps the model in sync across the boundary). Fallback on `error="session_missing"` (e.g. cleanup deleted JSONL between calls): rollover + retry once with the same prompt.

Registered as `#tool:dev-loop`.

## `tools/critical_review.py`

`CriticalReviewTool` — 3-pass adversarial review. Pass 1: radical-honesty analysis. Pass 2: adversarial challenge by different persona. Pass 3 (Synthesis): produces improved plan as `{name}-v2.md` (only when plan file referenced in task). Plan file resolution: file paths in CWD → wikilinks in vault. Cross-provider support via `#pass1:claude #pass2:gemini` tags. `_resolve_pass2_provider()` filters the tag through `dispatcher.policy_allows_provider(name, "critical-review")` before the registry lookup and logs policy rejection separately from an unknown name; both fall back to the primary provider. `_resolve_plan_file()` finds `.md` refs in task text. Pass 1 output saved as safety net (`-pass1.md`). Without plan file: 2-pass review-only. Output to `{cwd}/docs/critical-review-YYYYMMDD-HHMMSS.md` + `{plan}-v2.md`.

**Phase C2 capability switch**: when ALL passes use the same provider (no `#pass2:`-tag or `#pass2:` matches the primary) AND `provider.supports_sessions` AND `CLAUDE_SESSION_ENABLED`, all 3 passes share a session via `SessionContext` → cache hits across the chain. In session mode, Pass 2's adversarial prompt and Pass 3's synthesis prompt skip the explicit `pass1_output`/`pass2_output` text injection (would otherwise show prior passes twice — once via conversation history, once via inject). Cross-provider runs (e.g. `#pass1:gemini #pass2:claude`) keep the explicit-injection path because the provider switch IS the adversarial-perspective mechanism.

Registered as `#tool:critical-review`.

## `tools/security_audit.py`

`SecurityAuditTool` — 2-phase security audit + fix workflow. Phase 1 (read-only): scans for hardcoded secrets, command injection (`shell=True` + user input), path traversal, input validation gaps (null-byte, newlines), log injection, unsafe deserialization (`yaml.load` without SafeLoader), SSRF. Phase 2 (write): implements all fixes, runs `pytest`. Output to `{cwd}/docs/security-audit-YYYYMMDD-HHMMSS.md`. Registered as `#tool:security-audit`.

## `tools/scientific_investigation.py`

`ScientificInvestigationTool` — wissenschaftlicher Autopilot mit Audit-Trail-Pattern (Plan v5: `docs/plans/scientific-investigation-tool-v5.md`). **Status: Increments I0–I9 done (full pipeline)**.

**Engineering-Layer**: Tag-Parser für `#prior:`, `#cross-provider:none`, `#discipline:no-norms`, `#resume:`, `#engineering_reviewer:` mit korrektem `(?![-\w])` statt `\b` weil `\b` zwischen `e` und `-` matcht. Run-Isolation `docs/scientific-investigation-{ts}/{draft,traces,audit}/` + `.scientific-investigation/{run_id}/sub-tasks/`. Atomic state-write `.tmp` → rename. Manifest mit Git-SHA + Embedding-Modell + Tags.

**Pipeline**: Phase 0 (Framing + Bias-Statement + Cross-Investigation-Similarity) → Phase 0.5 (Pre-Registration mit norm/paper/telegram-Quellen, Disziplin-Warnung-Gate, Hash-Lock) → Phase 1 (Persona-Allocation: Author=primary, DA=cross, Methodiker=any_external; jede Allocation als `persona_allocation` Audit-Eintrag) → Phase 2 (Investigation-Plan + Multi-Persona-Review sequential, max 3 Iterationen; Author drafts → DA reviewt → Methodiker reviewt; bei P1-Findings Author-Rework; Konvergenz bei keinem P1) → Phase 3 (Execution-Loop: jeder Sub-Task läuft im Sub-CWD `{root_cwd}/.scientific-investigation/{run_id}/sub-tasks/{sub_id}/` mit `PYTHONPATH=root_cwd`; Default-Executor wrappt `DevLoopTool`; nach jedem Sub-Task werden Crosscheck-Files via `tests/crosscheck_*.py`-Glob entdeckt und über `externality_classifier` klassifiziert; `literature_search`-Sub-Tasks bekommen zusätzlich Adversarial-Citation-Search mit Levenshtein-Diversitäts-Check und Tool-Call-Verifikation als Audit-Hinweis) → Phase 4 (Synthesis draft in `draft/proof.md`) → Phase 5a (mechanical falsification check) → Phase 5b (cross-provider heuristic LLM review) → Phase 7 (engineering-reviewer rework loop, max 3 Iterationen) → Phase 8 (final Telegram-approval gate, bei approved Atomic-Rename `draft/proof.md` → `proof.md`) → Phase 6 (decision_log.md mit Status-Tuple, allen Phase-Summaries, Cherry-Picking-Block).

state.json persistiert `phase="phase8_done"` + Phase-spezifische Subdicts + `status_tuple`. error_code für Happy-Path: `pipeline_complete`.

**Status-Tuple** ist deterministisch über `compute_status_tuple` (Conjunction-Logik): `methodological_rigor=MEDIUM` IFF alle 6 Bedingungen (alle Pre-Reg-Schwellen extern, mind. 1×T2 pro Sub-Task, Adversarial-Diversity+Tool-Calls pass, Cross-Provider DA active, Engineering-Reviewer status="passed", User-Approval="approved") — sonst LOW. HIGH existiert NICHT im Schema (Plan §0.2 K15). Höchstes erreichbares `methodological_rigor=MEDIUM` (Same-Stack-Setup-Limitation explizit).

**tool.run() kwargs (injectable)**: `provider_lookup` (Allocator + Phase-2-Persona-Routing, default `dispatcher.policy_provider_lookup("scientific-investigation")` — policy-gefiltert, Kandidaten-Tupel unverändert; Tests setzen `lambda _: None` für hermetisches Verhalten), `sub_task_executor` (Phase-3-Executor, default `tools.scientific_investigation_phase3.default_devloop_executor`, in Tests via `monkeypatch.setattr` durch Stub ersetzt), `adversarial_query_generator` + `adversarial_search_executor` (optional, beide None → Adversarial-Search wird ausgelassen), `phase5a_measurements` (dict `{criterion_id: float}` overridet sub_state_cwd/measurements.json), `phase8_notify_callable` (default wrappt `notifier.send_message`; Tests mocken auf Inline-Stub), `phase8_timeout_sec` (override für `TOOL_SI_APPROVAL_TIMEOUT_HOURS * 3600`).

**Disziplin-Warnung**: wenn ALLE Schwellen `telegram_approval` als source haben, wird zweite Telegram-Approval mit `criterion_id="__discipline_warning__"` angefordert; bei Reject → RuntimeError + `phase05_failed`.

**Bypass-PolicyEngine-Routing**: `#cross-provider:none` rate-limited via `bypass_counter` (3/30d rolling); bei Limit-Überschreitung wird `PolicyEngine.request_approval()` gerufen — approved → run läuft + Bypass wird verrechnet, denied/timeout → `error_code="policy_denied"`. Jede Bypass-Aktion (mit/ohne PolicyEngine-Routing) erzeugt einen `cross_provider_bypass` Audit-Eintrag mit `policy_routed: bool` + ggf. `policy_response`.

Registered als `#tool:scientific-investigation`.

**Sub-Modul-Layout** (`tools/crosschecks/`):
- `audit_trail.py` — append-only JSONL via `open("a")` + Entry-Typ-Validierung (`crosscheck_tier` rejected ohne `file_path`+`file_sha256`, `assigned_tier` muss in `{T2,T3}` sein, T1 entfällt aus v5). Entry-Typen: `preregistration_threshold`, `preregistration_warning`, `crosscheck_tier`, `investigation_approval`, `cross_provider_bypass`, `persona_allocation`, `execution_sub_task`.
- `externality_classifier.py` — deterministisch: T2 nur wenn Audit-Eintrag existiert UND SHA256 noch passt UND `source ∈ {norm_reference, paper_reference, telegram_approval}`. `user_chat_signoff` ist im audit_trail erlaubt aber NICHT in `T2_ELIGIBLE_SOURCES`, sonst T3 default.
- `bypass_counter.py` — rolling-window-Counter in `.scientific-investigation/tool-bypass-counter.json`, atomic JSON-Write via `.tmp` rename, tolerant gegen korrupte Dateien.
- `similarity_index.py` — TF-IDF-like Jaccard-Cosine mit Stdlib `_tokenize`/`_RE_DELIMITERS`, append-only Liste in `.scientific-investigation/investigation-similarity-index.json`, idempotent auf `run_id`, `find_similar_investigations` excluded `exclude_run_id` damit ein Run sich nicht selbst matched. Threshold-Kalibrierung DEFERRED bis 20+ Investigations laut Plan §8.2.
- `cherrypicking_detector.py` — `build_cherrypicking_block(root_cwd, framing_text, run_id, threshold=None)` returns Markdown-Block mit Cosine-Sim-Hits (excludes self), inkl. Embedding-Modell-Doku (K11 Sichtbarkeit). `build_persona_allocation_block(allocations)` rendert die Allocation-Tabelle mit `✓`/`—` für cross-provider-satisfied. `write_decision_log(run_dir, run_id, sections)` schreibt decision_log.md aus `(heading, body)`-Tuples — leere bodies werden zu `*(noch nichts dokumentiert)*` damit die Struktur stabil bleibt.

**Phase modules**:
- `scientific_investigation_phases.py`: `phase_framing(task, provider, ...)` ruft LLM mit `_FRAMING_PROMPT` (YAML-Schema: question/hypothesis/bias_statement/discipline/framing_text), parsed via `_parse_yaml_minimal` (Pyyaml wenn verfügbar sonst Mini-Parser), schreibt similarity-index-Eintrag und liefert `FramingResult` mit `similarity_hits` zurück. `phase_prereg(framing, provider, ...)` validiert sequentielle IDs (F1, F2, ... ohne Lücken), max 5 Schwellen, `_validate_reference` verlangt für norm/paper mind. 20 chars + Ziffer + (für paper) DOI-Substring `10.\d{4,9}/`; bei `telegram_approval` blockiert auf `PreRegApprovalManager.request_threshold_approval` (Threading.Event-Pattern wie PolicyEngine). Disziplin-Warnung-Gate: wenn keine Schwelle norm/paper hat, wird Sentinel-Approval mit `criterion_id="__discipline_warning__"` angefordert; reject → RuntimeError. `compute_prereg_hash(thresholds)` ist deterministisch (sortiert nach criterion_id, JSON-canonicalized) — wird in plan.md frontmatter `prereg_hash:` geschrieben. `write_plan_md` rendert die kombinierte Phase-0+0.5-Doku. `phase_persona_allocation(...)` weist Personas Providern zu (Author→primary, DA→cross, Methodiker→any_external, fällt sequentiell auf primary zurück wenn keine cross-providers verfügbar); `provider_lookup` injectable.
- `scientific_investigation_phase2.py`: `phase_investigation_plan_review(...)` führt sequentially Author→DA→Methodiker (mit injizierten Per-Persona-Providern via Allocations + lookup). Bei P1-Findings → Author-Rework mit Findings-Block im Prompt. `_parse_investigation_plan` validiert sequentielle S1/S2/..., max 8 Sub-Tasks, jeder mit `addresses_criteria` Pflicht-Mapping zu Pre-Reg-Schwellen. `_parse_review_findings` toleriert leere Listen + ungültige Severities (→ P3 fallback). `Phase2Result` carries `plan + findings_by_iteration + iterations_used + converged + has_open_p1()`. Writer-Helpers `write_investigation_plan_md` + `write_review_findings_md`. Timeouts: `TOOL_SI_PHASE2_AUTHOR_TIMEOUT_SEC=1800` + `TOOL_SI_PHASE2_REVIEW_TIMEOUT_SEC=1200` + `TOOL_SI_PHASE2_MAX_ITERATIONS=3`.
- `scientific_investigation_phase4.py`: `phase_synthesis` ruft Author-LLM mit `_SYNTHESIS_PROMPT` (alle 5 Limitations-Kategorien-Pflicht + Falsifikations-Tabelle), schreibt `draft/proof.md`. `validate_synthesis_output` prüft Header-Position, alle 5 Kategorien, mind. 2 Sätze pro Kategorie, Black-List-Phrasen (`vernachlässigbar` etc). `compute_status_tuple` ist die Konjunktions-Logik aus Plan §2.5. `StatusTuple.methodological_rigor` ist `MEDIUM|LOW` (Literal-Type, HIGH strukturell ausgeschlossen).
- `scientific_investigation_phase5.py`: `phase_mechanical_falsification_check` parsed Schwellwert-Strings (`"5%"`/`"10mm"`), evaluiert `|measured| <= threshold`, merged Measurements aus optional-kwarg ODER sub_state_cwd/measurements.json sidecar. `phase_heuristic_review` ist cross-provider LLM-Review von proof.md + Phase-5a-Bericht. `Phase5aReport.aggregate_status` → `defined_criteria_all_within_tolerance` | `..._some_outside_...` | `..._all_outside_...` | `criteria_not_testable`.
- `scientific_investigation_phase7.py`: `phase_engineering_reviewer` ist cross-provider Rework-Loop (`#engineering_reviewer:<alias>` tag honored, sonst pickt aus `("openrouter","gemini","codex","claude")` — fällt auf primary mit `cross_provider_satisfied=False` zurück wenn keine cross-provider verfügbar). P1-Findings → BLOCKER → Author-Rewrite über primary; P3 → HINT (decision-log only). Max 3 Iterationen, dann `status="needs_revision"`.
- `scientific_investigation_phase8.py`: `phase_final_approval` sendet Telegram-Summary (status-tuple + top-3 limitations + draft-path), blockt auf `PreRegApprovalManager` mit Sentinel `INVESTIGATION_CRITERION="__investigation__"`, bei `approved` atomic-rename `draft/proof.md` → `proof.md`, audit-entry `investigation_approval` mit `telegram_msg_id` + `status_tuple_at_approval`. Rejected/timeout lässt draft bestehen.

**Other support modules**:
- `tools/scientific_investigation_approvals.py`: `PreRegApprovalManager` Singleton (via `get_manager()`), keyed by `(run_id, criterion_id)` — Phase-8-Final-Approval nutzt Sentinel `INVESTIGATION_CRITERION="__investigation__"`. `request_threshold_approval` blockt auf `threading.Event`, `respond` (vom Telegram-Listener aus aufgerufen) setzt das Event. `cancel_all` weckt alle Waiter mit `"skipped"` (Shutdown-Pfad). `reset_manager_for_tests` setzt das Singleton zurück (Test-Helper).
- `tools/sub_tool_context.py`: `build_sub_env(root_cwd)` prepends `root_cwd` to PYTHONPATH (idempotent — falls bereits an Position 0, unverändert). `ensure_sub_state_dir(root_cwd, run_id, sub_task_id)` erstellt `{root_cwd}/.scientific-investigation/{run_id}/sub-tasks/{sub_task_id}/`.
- `tools/personas/`: `base.Persona` (frozen dataclass mit role/provider_preference/system_prompt), `base.PersonaAllocation` (persona + concrete provider_name + cross_provider_satisfied bool). `author.AUTHOR` (primary), `devils_advocate.DEVILS_ADVOCATE` (cross), `methodiker.METHODIKER` (any_external). `ALL_PHASE2_PERSONAS` Tupel in Allocator-Reihenfolge. Engineering-Reviewer ist NICHT in ALL_PHASE2_PERSONAS — die Phase-7-Persona ist separat.
- `scripts/build_audit_pack.py`: standalone CLI für I9 — `python scripts/build_audit_pack.py --run-id <run_id> [--cwd <dir>] [--output <zip>]` discoveryt Run via manifest.json-Glob, baut ZIP mit kompletter `docs/scientific-investigation-{ts}/` Hierarchie + `audit_pack_meta.json` (build_ts, code_commit_sha, python_version, embedding_model, mechanism_hashes via SHA256 der scientific_investigation*-Module + crosschecks/*.py, requirements_lock). Exit-Codes: 0 ok, 2 no match, 3 multiple. Tolerant gegen fehlende git/requirements.txt.

**Config-Konstanten** (alle in config.py): `TOOL_SI_BYPASS_LIMIT_PER_30_DAYS=3`, `TOOL_SI_ADVERSARIAL_LEVENSHTEIN_MIN=8`, `TOOL_SI_CHERRYPICKING_SIMILARITY_THRESHOLD=0.7`, `TOOL_SI_PHASE0_TIMEOUT_SEC=600`, `TOOL_SI_PHASE0_5_TIMEOUT_SEC=900`, `TOOL_SI_TELEGRAM_APPROVAL_TIMEOUT_SEC=1800`, `TOOL_SI_PHASE7_TIMEOUT_SEC=1800`, `TOOL_SI_PHASE7_MAX_REWORK_ITERATIONS=3`, `TOOL_SI_APPROVAL_TIMEOUT_HOURS=24`, `TOOL_SI_LIMITATIONS_BLACKLIST` (11 phrases lower-case), `TOOL_SI_LIMITATIONS_REQUIRED_CATEGORIES` (5 categories), `TOOL_SI_LIMITATIONS_MIN_SENTENCES_PER_CATEGORY=2`, `TOOL_SI_EMBEDDING_MODEL="all-MiniLM-L6-v2"` (explizit dokumentiert im Manifest).

## `tools/brainstorm.py` + `tools/brainstorm_phases.py`

`BrainstormTool` — multi-persona Brainstorming-Round-Table mit domain-aware Personas.

**Phasen**:
- **Phase 0** (Topic-Analyse): LLM generiert 4-6 Personas mit unique `system_prompt` ≥100 chars über `phase_topic_analysis`. Schema-Validierung in `parse_personas`: kebab-case keys, unique, Count im Range.
- **Phase 0.5** (Provider-Allocation via `phase_provider_allocation`): default primary-only, `#cross-provider`-Tag aktiviert Round-Robin über `(claude, gemini, codex, openrouter)`; degradiert auf primary wenn keine Cross-Provider verfügbar.
- **Phase 1** (initial): jede Persona produziert max `TOOL_BS_MAX_IDEAS_PER_PERSONA_PER_ROUND=10` Ideen via `phase_idea_generation`.
- **Phase 2** (Cross-Pollination): jede Persona sieht Peer-Ideen, contribute Aufbau/Synthese/Challenge/Gap-Ideen via `phase_cross_pollination`. Truncation pro Peer `TOOL_BS_MAX_PEER_CHARS_PER_PERSONA=8000`, total `TOOL_BS_MAX_TOTAL_INJECT_CHARS=50000`.
- **K (Konvergenz)**: `cluster_ideas` greedy single-pass Jaccard-Cosine ≥ `TOOL_BS_CLUSTER_SIMILARITY_THRESHOLD=0.4`. `check_convergence` Stop wenn `new_clusters/total < TOOL_BS_CONVERGENCE_THRESHOLD=0.20` AND `previous_count > 0` — Runde 1 ist NIE konvergiert. Hard-Cap `TOOL_BS_MAX_ITERATIONS=5`.
- **Phase 3** (Synthesizer via `phase_synthesis`): Primary-Provider wählt Top-`TOOL_BS_DEFAULT_TOP_N=5` mit Pro/Contra/Nächster-Schritt.

**Output** `{cwd}/docs/brainstorm-YYYYMMDD-HHMMSS.md` mit Topic, Persona-Liste, Synthese, Cluster-Map und Iterations-Statistik. State + Per-Iteration-Files in `{cwd}/.brainstorm/{ts}/` (atomic `.tmp → rename` JSON, phase-tagged `started|phase0_done|phase05_done|iter{N}_done|iterations_done|synthesis_failed|complete`).

**Tags**: `#cross-provider` (opt-in Round-Robin), `#max_iterations:N` (clamp 1-10), `#top_n:N` (1-20), `#min_personas:N` / `#max_personas:N` (2-10; `min > max` wird auto-korrigiert auf `max=min`).

**Implementation-Details**: Prompts nutzen `.replace`-Substitution (NICHT `.format`) damit braces im User-Topic nicht crashen. Per-Persona-Failure ist non-fatal — Tool continued mit Rest, leerer-`ideas`-Edge-Case → `error_code="no_ideas"`. Capacity exhausted vor Phase 0 → `retryable=True` ohne LLM-Call; nach Phase 0 → State+Partial-Ideen gespeichert, `retryable=True`. Empty-Topic-Guard (alle Tags entfernt → leerer String) → `error_code="empty_topic"` ohne LLM-Call. ToolTracer emittet `run_start`, `phase_start/end`, `iteration_start/end`, `subprocess_call/result`, `capacity_exhausted`, `run_end`. `read_only=True` (alle LLM-Calls), Report-File geht via `_write_tool_file` raus. `provider_lookup` kwarg injizierbar für Tests (default `dispatcher.policy_provider_lookup("brainstorm")` — der Round-Robin läuft nur über Provider, die `tool_providers` erlaubt; unter `[claude, codex]` also über zwei statt vier). YAML-Parsing via `_extract_yaml_block` + `_parse_yaml_minimal` aus `scientific_investigation_phases` — Prompt verlangt single-line quoted `system_prompt` weil der Fallback-Parser keine `|`-Block-Skalare versteht.

Registered als `#tool:brainstorm`.

## `tools/deep_security_audit.py`

`DeepSecurityAuditTool` — multi-agent deep security audit with 6 expert personas + CISO synthesis + optional fix.

**Phase C1 capability switch** (`run()` → `_run_subagent_mode` vs. `_run_sequential_mode`): when `provider.supports_sessions` AND `CLAUDE_SESSION_ENABLED`, a single Claude subprocess spawns the 6 personas IN PARALLEL via the **Agent** tool (not `Task` — every `Task*` name is a task-LIST tool and spawns nothing, so a prompt naming it degraded the fan-out silently to a monolithic run; the master call uses `read_only=False`/`--dangerously-skip-permissions`, so this was the *wrong tool name in the prompt*, **not** an `--allowedTools` denial — that variant applies to the read-only phases), writes per-persona reports + the CISO synthesis as files via the Write tool, and returns a brief executive summary as the final assistant message. Phase 8 (Fix) stays a separate subprocess in either mode (write-phase, no shared-context benefit). Sequential mode (today's behaviour) is the fallback when the flag is off or the provider lacks session support.

**Phase 6.5 — Round-Table dialogue** (opt-in via `#roundtable` tag): inserted between Phase 6 (last agent) and Phase 7 (CISO synthesis). Each persona reads the OTHER 5's findings and contributes Agreements/Rebuttals/Extensions/Gaps to a `deep-security-audit-{ts}-roundtable-{key}.md` report. The CISO prompt receives a structured `roundtable_block` summarising the cross-expert validation. `#roundtable` forces sequential mode (subagent-mode bypasses the per-persona context-injection that makes round-table work). Failed agents (Phase 1-6) are skipped during round-table with a `[skipped]` placeholder. Adds up to 6 extra subprocess calls — vorhersehbare Kosten. Reuses `TOOL_DSA_MAX_AGENT_OUTPUT_CHARS`/`TOOL_DSA_MAX_TOTAL_INJECT_CHARS` for truncation; keine neuen Config-Konstanten.

**Agents**: (1) Penetration Tester — exploit chains, privilege escalation, prompt injection, sandbox escape, (2) Security Architect — trust boundaries, data flow, threat model, defense-in-depth, (3) Code Auditor (SAST) — OWASP/CWE patterns, line-by-line scanning, CWE mapping, (4) Supply Chain Analyst — dependencies, imports, CLI tool hijacking, third-party risk, (5) Data & Privacy Analyst — secrets management, PII exposure, credential handling, log leakage, (6) Forensics/IR Specialist — audit trails, log integrity, anomaly detection, evidence preservation.

**Phase 7 (CISO synthesis)**: deduplicates, cross-validates, builds attack chains, creates prioritized remediation roadmap. **Phase 8 (optional)**: fix implementation. `#no-fix` tag skips fix phase. Individual agent reports saved as `{cwd}/docs/deep-security-audit-{ts}-{agent}.md`. Combined report: `{cwd}/docs/deep-security-audit-YYYYMMDD-HHMMSS.md`. Graceful degradation: continues if individual agents fail, saves partial results on capacity exhaustion.

Registered as `#tool:deep-security-audit`. Config: `TOOL_DSA_AGENT_TIMEOUT_SEC` (30min per agent), `TOOL_DSA_SYNTHESIS_TIMEOUT_SEC` (40min), `TOOL_DSA_FIX_TIMEOUT_SEC` (60min), `TOOL_DSA_MAX_AGENT_OUTPUT_CHARS` (15k), `TOOL_DSA_MAX_TOTAL_INJECT_CHARS` (80k).

## `tools/pr_babysitter.py`

`PRBabysitterTool` — polls open GitHub PRs via `gh` CLI and queues fix tasks. Two modes selected via `#pr-mode:queue|report-only` (default `queue`):

- **queue mode** (P2): appends a `- [ ] PR #N (<repo>) review-feedback verarbeiten cwd:<cwd> #tool:dev-loop` queue item per PR with new triage signal (new comments OR CI flipped to failure OR failure on new commit).
- **report-only mode** (P5): sends a Telegram message per change `🔍 PR-Babysitter: PR <repo>#<N> — <reason>` with embedded `/pr-fix <repo>#<N>` and `/pr-ignore <repo>#<N>` slash commands so the user decides whether to act. Module-level helpers `cmd_pr_fix()` / `cmd_pr_ignore()` are exposed for the telegram_listener handlers (no inline-keyboard infrastructure yet).

Tags: `#repos:owner/r1,owner/r2` (overrides `PR_BABYSITTER_REPOS` env var), `#pr-labels:auto-fix,bot-ok` (whitelist filter, only PRs carrying ≥1 label survive), `#pr-mode:queue|report-only`. State persists at `{cwd}/.pr-babysitter/state.json` (schema: `{prs: {<owner/repo#N>: {last_seen_comment_count, last_check_status, last_seen_commit_sha, last_queued_at, last_ignored_at, last_checked_at}}}`). `PR_BABYSITTER_QUEUE_COOLDOWN_HOURS` (default 1h) suppresses duplicate queue items for the same PR within the window.

Tool itself is `read_only=True` — it only appends queue items / sends Telegram; the downstream `dev-loop` does the actual repo write. `gh_helpers.list_open_prs()` / `view_pr()` handle the CLI calls; `gh_not_found`/`gh_auth` errors surface as `error_code="gh_unavailable"` in the ToolResult. Registered as `#tool:pr-babysitter`.

## `ci_watcher.py`

P4 CI-Failure-Watcher — module-level `sweep_once(repos, *, cooldown_hours, list_failed_runs_fn, append_task_fn) → summary` polled from the `check-ci-failures` heartbeat handler.

- Repo whitelist from `CI_WATCHER_REPOS` env var (semicolon-separated `owner/name`). Empty list disables the handler entirely (`_check_ci_failures` returns `None` immediately, no gh call).
- Dedup is per-`(repo, headSha)` — multiple failed checks on the same commit produce **one** queue item. Per-SHA marker persists in `logs/ci-watcher-state.json`.
- Cooldown of `CI_WATCHER_QUEUE_COOLDOWN_HOURS` (default 2h) blocks re-queueing the same SHA even if the state file gets reset.
- Queue line format: `CI failure (<repo>@<branch>, <sha7>): <title80> [cwd:<local>] #tool:dev-loop`. `CI_WATCHER_REPO_PATHS` env var (`owner/repo=local/path;...`) optionally maps repos to local clones so the queue item carries a usable `cwd:`. Unmapped repos get a queue line without `cwd:` — user must edit before it runs.
- Heartbeat handler returns `None` (no Telegram noise) when nothing was queued AND no gh errors occurred.

## `gh_helpers.py`

Single subprocess wrapper around `gh` CLI used by `pr_babysitter.py` and `ci_watcher.py`. All public functions return `(data, error)` tuples — never raise. Typed errors: `gh_not_found` (binary missing), `gh_auth` (token expired / not authenticated), `gh_not_found_repo` (404), `gh_timeout`, `gh_error: <stderr>`. Public API: `gh_available()`, `list_open_prs(repo, *, timeout_sec, limit, labels=None) → (list[dict], err)`, `view_pr(repo, number, *, timeout_sec) → (dict, err)`, `list_failed_runs(repo, *, timeout_sec, limit) → (list[dict], err)`. Internal `_parse_json()` shared between callers, error-classification heuristics in `_run_gh()` (lowercased stderr keyword match — `"not logged"`/`"authentication"` → `gh_auth`, `"404"`/`"could not resolve"`/`"not found"` → `gh_not_found_repo`).

## `parallel_runner.py`

Existing parallel sub-agent runner extended with **worktree-isolation helpers** (P1). When the parent task carries `#worktree`, `run_parallel()`:

1. Pre-flights every CWD group: `_is_clean_git_repo(base_cwd) → (ok, reason)` runs `git rev-parse --git-dir` + `git status --porcelain`. Dirty trees / non-git dirs short-circuit the whole group with `provider_name="worktree"` failed results before any subtask spawns.
2. `_create_worktree(parent_cwd, worktree_id) → (path, err)` runs `git worktree add --detach <parent>/.worktrees/<id> HEAD`. `worktree_id = "parallel-" + sha1(parent_task|group_key|idx)[:8]` — short to fit Windows' 260-char `MAX_PATH` when nested deep.
3. Each subtask in the group has its `SubTask.cwd` rewritten to the worktree path before thread spawn.
4. `_remove_worktree(parent_cwd, wt_path)` runs `git worktree remove --force <path>` only when **all** subtasks of the group succeeded AND `#keep-worktree` is absent. On failure the worktree is **retained** and the path is appended to every failed subtask's `error` string (`... [worktree retained: <path>]`) for user inspection.

`PARALLEL_WORKTREE_ROOT` constant in `config.py` controls the subdir name (default `.worktrees`). Doctor check `check_worktrees()` scans `ALLOWED_CWD_ROOTS` for orphans and offers `git worktree prune` + remove via `--fix`.
